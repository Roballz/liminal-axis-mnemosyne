# 事项进展与 RRF 上下文偏好

2026-09-25，用户确认方案；实现状态为 implemented_unverified。

## 计划／悬念

- 保留原有创建、了结规则，使用 `content`、`currentProgress`、`remaining`（各50字内）。1.3.1 取消短标题的生成、编辑和展示，旧 `title` 不再参与派生；旧长内容不批量改写，注入取前50字。副模型逐楼摘要时只在正文确有进展时输出 `plans.update`；重复提及不冒充进展。
- 更新与关联均使用稳定计划ID，兼容模型返回的 p1/p2。按叶子顺序回放，因此较早截止点不读取后续进展。
- 主模型常驻注入使用 `p1. [计划/悬念] 内容`，附当前进展和仍待解决，不显示稳定ID。摘要整理和 Query 内部目录继续使用稳定ID，避免同名事项误关联。
- 计划卡片末行列出关联楼层，可定位摘要，不展开关联摘要正文。召回条目下只添加“关联悬念/计划：[类型] 内容”。卡片默认隐藏内部ID；设置 → 基本设置 → 显示内部编号（调试）可开启，不影响主模型注入。

## 摘要标签

- 逐楼叶子持久化 `tags.version=1`，包含 `participants`、`planIds`、`eventIds` 和手动 `public.reason`，并随对应 MemoryRevision 进入核心包。
- 人物省略表示未知，空数组表示未识别到在场者。名字沿用正文和已有目录的规范称呼；不把被提及/回忆中的人物当作在场者。
- 公开只能手动开启，必须填写理由。公开代表可能传播，不代表所有角色一定知情。
- 摘要多选支持批量公开，覆盖所选总结下的有效逐楼叶子；导入的整段历史没有逐楼标签，不批量赋予知情名单。统一理由可修改，已有在场人物及关联保留。先校验整批身份再写，一次重算、保存与索引调度。
- 自动事件标签只是检索相关，不改人工事件成员；正式成员也在召回时贡献同一关联信号。标签不拼进向量或 BM25 文本。
- 只改标签不变更叙事依赖：仅当来源、基线、正文、摘要内容和其他叙事字段完全一致时，旧依赖可指向同身份新版本。原有人工审核仍须匹配精确来源，后续相关编辑继续阻断。
- 旧摘要不自动付费补标；未标人物的摘要可以走高相关例外或最后补位。高层总结不把子摘要人物简单合并成所有事实的知情名单。

## 事件最新进展

- 事件模型输出最近真实进展的来源摘要ID及30字内小结。程序从来源摘要取剧情时间，不使用处理时间；重新措辞、再次提及不刷新时间。
- `latestProgress.version=1` 与事件修订一同保存。旧事件缺少该字段不再被判为概要待更新；真实新增成员、来源受阻和失效依赖仍按原规则待确认。明确无进展用 null。
- 事件编辑支持30字最新进展及对应关联摘要，时间从所选摘要取值；卡片在事件概要下显示最新进展。留空可清除已有进展，未修改的旧字段保留。
- 常驻状态块放在计划／悬念后，依次列“进行中”“暂搁”，使用跨组连续的 `e1. [事件] 标题`、剧情时间与短进展。结束、待审核或来源受阻事件不进入该块。归档只是界面组织，不改变这条规则。
- 原有详细事件概要和过程链仍只在相关摘要召回时展开。

## 召回顺序

1. Embedding、BM25 各按原配置取候选；用原始两路排名给全部已取到的去重候选计算 RRF，不先按重排上限截断。
2. 原始 RRF Top N 送 rerank，依原分数门槛决定是否升原文。现有“融合后重排候选上限”继续只作用于这一步，0 仍为跳过 rerank。
3. 先取达标原文，再取原 BM25 摘要名额。
4. RRF 摘要单独采用上下文偏好：命中任一当前人物、公开、Embedding 达高相关例外阈值、或处于原 BM25 前 N 名的摘要优先。组内按原 RRF 分数加有限事项加分排序；跳过已选摘要，不足再从其他候选补位。
5. 原 Embedding 榜按原阈值补剩余名额。四路共享去重与总额度。

Query 在原 INTENT/Q 后独立输出 CONTEXT（当前人物、高概率相关计划/悬念/事件ID），不能编造事项ID，不把ID拼进 Q；标识缺失或坏格式只关闭偏好，不破坏正常查询。

默认上下文偏好开启、高相关例外阈值0.9、BM25前5名例外、事项加分15%。三项可调，关联加分限制0～50%，多个事项命中只加一次；原始 RRF/Embedding/rerank 分数不改。调试面板展示准入理由、原RRF和偏好排序分。

## 保存和回退

1.3.1 摘要页事件目录按页面响应式版本共享一次校验，保存和注入仍保留精确宿主版本检查。折叠总结只在展开后挂载子卡；普通、搜索和选择列表首批40条，点击显示更多继续，全选和搜索的数据范围不受首批限制。

核心包升级为 v7，继续读取 v1～v6；新增字段随包导出恢复。旧代码不能读取 v7 新字段，应保留升级前备份。聊天中的计划增量仍沿用宿主 chat extra 保存，不改变既有核心包对 chat-state-delta 的排除声明。

关闭“RRF 人物／事件优先”恢复原召回排序；既有候选数、配额和重排上限不因升级自动调整。新的进展和标签字段可保留，不需删除数据。
