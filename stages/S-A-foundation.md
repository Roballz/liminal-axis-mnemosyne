# S-A：接入验证与工程地基

状态：in_progress。范围：T-00～T-03（含 T-02A 前置验证）。更新：2026-09-20。

## 1. 阶段目标

证明 Mnemosyne 能在隔离 TT 环境可靠接入生成链路，冻结自己的身份／版本契约，并建立可部署、可备份、可恢复的最小存储地基。

已接受先评估 B 本机版、后完善 A 独立服务器版；B/A 是部署方式，不是阶段编号。采用 B 不代表 A 已交付，正式存储 provider 仍须经过 T-03 选型和恢复验收。

## 2. 任务地图

| Task | 一句话目标 | 当前状态与完成标志 |
| --- | --- | --- |
| T-00 ✅ | 固定真实 TT／柏宝书／测试数据与副 API 基线 | verified；不再猜安装版本、存储模式或输入 |
| T-01 ✅ | 做最小 TT Adapter 探针 | verified；await、payload、取消与 request gate 边界已验证 |
| T-02A ✅ | TT 宿主身份与变更事件验证 | verified；stableId/integrity、Branch、Edit/Delete/Swipe/Regenerate、reopen/rename 边界 |
| T-02 ✅ | 身份、历史版本与上下文的最小可执行契约 | verified；1891043 最终复核关闭 R1～R5，范围限最小参考模型 |
| T-03 ▶ | 可迁移、可恢复的存储地基 | in_progress；G1已通过；P3有界基元/v1完整恢复增量待review，业务编译仍有界化未完成；自动维护受阻，继续仅限受控隔离 |

用户已授权 Chat 核对后发布正式卡并高难前置。旧 `tasks/T-03-storage-foundation.proposal.md` 为 superseded 历史入口；Codex 不修改关卡许可、不自动生成 T-04。G1 正式回执见 `notes/t-03-g1-final-review.md`；P3 最新专项回审及施工边界见 `notes/t-03-p3-maintenance-review.md`，均为原任务记录，不是新任务卡。

## 3. 阶段完成线

阶段 A 结束时：隔离 TT 生成前接入可行性已证实；核心身份／版本契约已到可编码且可测试状态；选定 provider 的最小持久化、逻辑导出与空环境恢复经过验证。

本轮 T-03 明确先验收隔离桌面 TT 存储地基：G1 选型/协议审查通过，随后有界清单、完整恢复、规定正确性/1x合成资源冒烟与操作说明闭环。只完成 P0～P2 不关闭 T-03；5x/10x 为容量探索，未测规模不得宣称可用。

手机独立准入，安全条件不具备可保留 mobile pending，但不得称为手机生产发布。A 服务器、真实 B→A 迁移尚未完成时单独注明，不以桌面或内存测试替代。

本阶段不要求完成正式剧情导入、自动记忆、完整混合召回、自定义模块或 MCP。第一个可日常使用的档案／原文搜索闭环仍属于阶段 B。

## 4. 已确认输入

历史 T-02A 基线见 `notes/baseline.md`：Windows 10 x64、TT 2.2.0 dev/Canary（git `5e33bf6fead6`）、柏宝书 1.2.9 纯前端本地模式、`system @d0` 注入配置及当时副 API／embedding／rerank 模型。

T-03 P0 新证据：用户更新后的 TT 为 `dev (367b0c7e9410)`，已在 `.t03-local/tt-367b0c7` 独立便携副本/data root/WebView profile 验证 api.db；具体程序哈希、隔离过程与合成库清单见正式 T-03 实施回报。旧5e33记录不代表当前数据库安装，源码支持也不替代实测。

2026-09-20用户提供候选新版 `c90d77d50166`。Chat 对比固定的 `367b0c7...c90d77d` 源码，确认同步维护锁/文件时间处理有变动，但 db.js/types未变、原生Execute仍按namespace分发，没有旧打开代次校验。**新版二进制尚未在本次review实测，不因此更新安装基线或宣布宿主缺口已修复。**

用户已有电脑测试聊天与 ST／TT 导出能力；现用手机不用于首轮破坏性实验。长期 RP 主要使用单台手机，B 版单权威写入/明确交接已确认，不重复询问。

T-02 不沿用柏宝书 Leaf 作为正式对象名；普通派生按 User + Assistant 一轮一份，原文单独归档不意味着 User 单独摘要。

## 5. 已确认方案与本轮落实

- **Head 已接受。** `Branch.head_snapshot_id` → `hs_<UUIDv4>` HistorySnapshot → 有序分块共享 HistoryManifest。固定父快照前缀及锚；父线改旧楼不改变子线，回滚建新快照，正文与记忆版本分离。见 `docs/08-history-snapshot-and-rebuild.md`。
- **正文权威与逐层重建。** 当前分支正文为已发生剧情依据；状态等为派生。匹配适用回合复用，受影响总结/事件/累计状态按轻量 input_refs/coverage 重建，不另建状态权威。
- **T-02 最终 review。** 审核实现 `1891043` 关闭 R1～R5；独立执行范围、Codex 回报与未验证边界见 `notes/t-02-final-review.md`，本轮不重报为新执行结果。
- **存储必须继承的契约。** 06 v0.3 / schema_version=1 / 逻辑包v2及Node参考模型；固定检查点与 current 选择、分支 corrections、R5 根版本不得同时被纠正、derived-only 同线前缀续用、跨Story重绑拒绝及幂等均不可被存储实现削弱。
- **B/A 共用核心。** UUID、来源与正确记忆可迁移；Trivium NodeId/TQL不作领域身份，不因换库全量重摘。当前v2不是未来全产品备份；只验已建模对象及明确版本化的必要恢复材料。
- **T-03 高难前置。** P0必要核对 → P1发布/幂等/恢复协议 → P2极小真实TT故障闭环 → G1 → P3有界清单/完整一致恢复 → P4索引与资源/设备验证 → P5诊断和说明。先证实最危险路径，再扩大CRUD/界面。
- **G1 最终 review 通过。** 审核 `48663c4`；成功关闭 owner 永久失效，关闭失败保留唯一登记/队列，异常 root/payload 不再当作空库。Chat 对齐15个文件blob后独立运行84项存储回归及15文件语法检查，全部通过；同一最终19项定向测试在实际旧实现上10通过/9失败。完整150项/23文件语法/diff为Codex结果；原生小验证为已审阅的隔离TT记录，不混称Chat真机执行。详见 `notes/t-03-g1-final-review.md`。
- **B1 阶段性选型。** 现有原语及有限故障证据足以支持继续以 TT TriviumDB 做 P3；只批准下一增量，不把当前硬上限原型批准为手机生产库，不提前关闭外部维护、持久请求与规模问题。
- **P3专项回审。** 接受15b8605的停工理由：原生旧handle在同名库重开后仍可写，不是G1已修复的自有owner再次复活。Codex的211项及旧TT原生记录保留；本轮仅做源码/证据核对，未独立复跑211项、未运行新版TT，不对整个P3宣称验收通过。

文件优先级：用户最新确认 → 当前正式 task/本阶段导读及最新专项review → 已验收06/08与T-02/G1最终review → 09协议/07首轮候选/旧proposal及早期架构示意。最新专项review为 `notes/t-03-p3-maintenance-review.md`，覆盖原交接中“其余P3能否继续待决定”的部分；不抹改原始运行证据。

## 6. 当前阻塞、关卡与未决项

**T-02 无剩余验收阻塞；G1-R1/R2仍已关闭。P3自动外部维护隔离受阻，c90d77d未关闭该缺口。** `notes/t-03-p3-handoff.md`保留原始停工证据。当前P3有界结构基元与v1辅助格式恢复增量为implemented_unverified；普通领域编译仍用P2全库oracle，分页发布/checkpoint与规模化恢复未实现，不把T-03升为verified。

P3后续工作与许可：

1. 持久请求/生成ID前置诊断已有实现，仍保留P2上限；本轮补齐v1完整辅助材料恢复与新intent格式的隔离进程故障验证；有界基元尚需接入领域编译、发布和恢复checkpoint。具体运行证据见evals/t03/p3-structures-recovery，不能以正常close/reopen代替强杀验证。
2. 自动宿主维护路径继续返回HOST_MAINTENANCE_UNSUPPORTED。推荐与TT作者确认原生打开代次/租约或可等待的维护前排空能力；当前只为提议，不授权修改TT或自动切B2/A。新版维护锁不能代替旧句柄代次隔离，JS写前检查不能代替原子校验。
3. **可继续其余P3工程工作**：有界manifest/command/view/expected/journal/目录、精确枚举、完整一致备份恢复；只用已确认的独立实例/namespace及合成数据，明确无未经协调的宿主同步/归档/库替换。合作式维护必须先等待suspend再操作。无法确保这个隔离前提的原生测试继续停，可先做纯模型/独立测试。

这不是批准“靠用户记得关同步”的生产模式；自动维护安全、完整T-03及手机准入仍有门禁。不得改开关假称已具备fencing，不以换到c90d77d为由移除门禁。P3完成后先交高难项review，再按原卡安排P4/P5；S05、完整流式恢复、索引恢复、规模和手机均未验收。

性能预算尚无用户确认，先探索测量；正确性不得打折，不能测后自设阈值宣布手机达标。产品层仍待后续：非标准回合、品牌名、模糊导入匹配、自动保留/清理、完整模板。不在T-03顺手决定；数据先保留，不自动GC。

## 7. 需要用户提供

本次专项回审无需用户再确认已有产品规则。其余P3仍沿用隔离合成样本；模型、设备路径、单端偏好不重复索要。

上游接口增强或新的受限产品模式是后续待决定项，不由Codex自行承诺。需新的安装/依赖、升级隔离版本或手机测试时，先给出已核查事实和最小授权请求。真实RP、付费API、生产手机升级、强杀共享实例不在默认许可内；不删除旧证据库。

## 8. 阶段收尾 review

T-00、T-01、T-02A、T-02已verified；T-03为in_progress，G1通过。P3部分诊断实现未全面验收，自动维护受阻；其余P3仅在明确受控环境继续。隔离桌面的P2原型和原故障边界已复核；完整有界存储、性能和手机生产使用未验收。
