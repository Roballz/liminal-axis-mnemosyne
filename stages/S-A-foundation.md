# S-A：接入验证与工程地基

状态：in_progress。范围：T-00～T-03（含 T-02A 前置验证）。更新：2026-09-20。

## 1. 阶段目标

证明 Mnemosyne 能在隔离 TT 环境可靠接入生成链路，冻结自己的身份／版本契约，并建立可部署、可备份、可恢复的最小存储地基。

已接受先评估 B 本机版、后完善 A 独立服务器版；B/A 是部署方式，不是阶段编号。采用 B 不代表 A 已交付，正式存储 provider 仍须经过 T-03 选型和恢复验收。

## 2. 任务地图

| Task | 一句话目标 | 当前状态与完成标志 |
| --- | --- | --- |
| T-00 ✅ | 固定真实 TT／柏宝书／测试数据与副 API 基线 | verified；不再猜安装版本、存储模式或输入 |
| T-01 ✅ | 做最小 TT Adapter 探针 | verified；await、payload、取消与 request gate 边界已验证 |
| T-02A ✅ | TT 宿主身份与变更事件验证 | verified；stableId/integrity、Branch、Edit/Delete/Swipe/Regenerate、reopen/rename 边界 |
| T-02 ✅ | 身份、历史版本与上下文的最小可执行契约 | verified；1891043 最终复核关闭 R1～R5，范围限最小参考模型 |
| T-03 ↺ | 可迁移、可恢复的存储地基 | in_progress；R1/R2返修及150项Node/隔离TT小验证已完成；待Chat review，G1未放行 |

用户已授权 Chat 核对后发布正式卡并高难前置。旧 `tasks/T-03-storage-foundation.proposal.md` 为 superseded 历史入口；Codex 不修改关卡许可、不自动生成 T-04。

## 3. 阶段完成线

阶段 A 结束时：隔离 TT 生成前接入可行性已证实；核心身份／版本契约已到可编码且可测试状态；选定 provider 的最小持久化、逻辑导出与空环境恢复经过验证。

本轮 T-03 明确先验收隔离桌面 TT 存储地基：G1 选型/协议审查通过，随后有界清单、完整恢复、规定正确性/1x合成资源冒烟与操作说明闭环。只完成 P0～P2 不关闭 T-03；5x/10x 为容量探索，未测规模不得宣称可用。

手机独立准入，安全条件不具备可保留 mobile pending，但不得称为手机生产发布。A 服务器、真实 B→A 迁移尚未完成时单独注明，不以桌面或内存测试替代。

本阶段不要求完成正式剧情导入、自动记忆、完整混合召回、自定义模块或 MCP。第一个可日常使用的档案／原文搜索闭环仍属于阶段 B。

## 4. 已确认输入

历史 T-02A 基线见 `notes/baseline.md`：Windows 10 x64、TT 2.2.0 dev/Canary（git `5e33bf6fead6`）、柏宝书 1.2.9 纯前端本地模式、`system @d0` 注入配置及当时副 API／embedding／rerank 模型。

T-03 P0 新证据：用户更新后的 TT 为 `dev (367b0c7e9410)`，已在 `.t03-local/tt-367b0c7` 独立便携副本/data root/WebView profile 验证 api.db；具体程序哈希、隔离过程与合成库清单见正式 T-03 实施回报。旧5e33记录不代表当前数据库安装，源码支持也不替代实测。

用户已有电脑测试聊天与 ST／TT 导出能力；现用手机不用于首轮破坏性实验。长期 RP 主要使用单台手机，B 版单权威写入/明确交接已确认，不重复询问。

T-02 不沿用柏宝书 Leaf 作为正式对象名；普通派生按 User + Assistant 一轮一份，原文单独归档不意味着 User 单独摘要。

## 5. 已确认方案与本轮落实

- **Head 已接受。** `Branch.head_snapshot_id` → `hs_<UUIDv4>` HistorySnapshot → 有序分块共享 HistoryManifest。固定父快照前缀及锚；父线改旧楼不改变子线，回滚建新快照，正文与记忆版本分离。见 `docs/08-history-snapshot-and-rebuild.md`。
- **正文权威与逐层重建。** 当前分支正文为已发生剧情依据；状态等为派生。匹配适用回合复用，受影响总结/事件/累计状态按轻量 input_refs/coverage 重建，不另建状态权威。
- **T-02 最终 review。** 审核实现 `1891043` 关闭 R1～R5；独立执行范围、Codex 回报与未验证边界见 `notes/t-02-final-review.md`，本轮不重报为新执行结果。
- **存储必须继承的契约。** 06 v0.3 / schema_version=1 / 逻辑包v2及Node参考模型；固定检查点与 current 选择、分支 corrections、R5 根版本不得同时被纠正、derived-only 同线前缀续用、跨Story重绑拒绝及幂等均不可被存储实现削弱。
- **B/A 共用核心。** UUID、来源与正确记忆可迁移；Trivium NodeId/TQL不作领域身份，不因换库全量重摘。当前v2不是未来全产品备份；只验已建模对象及明确版本化的必要恢复材料。
- **T-03 高难前置。** P0必要核对 → P1发布/幂等/恢复协议 → P2极小真实TT故障闭环 → G1 → P3有界清单/完整一致恢复 → P4索引与资源/设备验证 → P5诊断和说明。先证实最危险路径，再扩大CRUD/界面。
- **G1 首轮 review。** 审核实现 `3e583e7`、任务修正 `8e3f6a6`。Chat 在隔离 Linux/Node v22.16.0 对齐14个文件blob后复跑65项存储测试及14文件语法，全通过；另外4个组合诊断复现下述两项阻塞。131项全套/21文件语法与diff检查为Codex回报，本轮未独立重跑48项契约、18项旧探针或TT真机。原生强杀/小包证据保留为已审阅的有限现场记录。

文件优先级：用户最新确认 → 当前正式 task/本阶段导读及 `notes/t-03-g1-review.md` → 已验收06/08与T-02最终review → 09候选协议/07首轮候选/旧proposal及早期架构示意。历史文档中“T-03仍proposal”的语句描述当时状态，不覆盖正式发布；冲突需报告，不能静默改产品语义。

## 6. 当前阻塞、关卡与未决项

**T-02 无剩余验收阻塞；T-03 G1 尚不放行。** 继续原 T-03 的 P0～P2 返修，先读 `notes/t-03-g1-review.md`：

- **G1-R1（优先）：owner 生命周期失守。** 同api/namespace中旧owner关闭后被移出registry，却能recover，与新owner形成两个可写队列；隔离模拟中两次竞争写入均确认，重开丢失其中一次提交及新增分支/故事。关闭失败同样可复现。这不是跨设备/跨WebView范围外问题。
- **G1-R2：无效发布信息被当成空库。** root缺tip或原生节点存在但payload为null时，恢复会返回ready和空历史/账本。需区分记录不存在与无效记录，完整校验root，错误保留为待修复而非空库成功；合法初始/暂存/未提交孤儿仍须支持。

修复并补定向测试后再交 G1 review，不进入P3～P5，不将部分进度升为总任务完成。仅需相关生命周期/异常记录的隔离原生小验证；发布协议未实质改变时不重复整套强杀矩阵。无须立即切B2/A或推翻全部协议。

2026-09-19返修验证检查点：基线26f4171，关闭成功的owner永久终止、失败保留唯一协调归属；恢复严格区分缺记录与损坏payload/root。Node原131+新增19共150项通过；隔离TT `20260919g1` 证明旧owner拒绝复活、缺tip/null payload拒绝且物理材料20→20；23个相关源码文件语法检查和diff检查通过。完整状态和证据见正式task末节；两项仍待Chat复核。

**后续P3条件，不扩大本次返修：** 持久化原请求/生成ID的正式入口、外部维护换代协调、command/view/expected/journal/目录有界化与完整备份格式。B1仍为候选，未批准生产使用；现有小样本硬上限可保留，S05、流式恢复、索引恢复、规模和手机仍未完成。

性能预算尚无用户确认，先探索测量；正确性不得打折，不能测后自设阈值宣布手机达标。产品层仍待后续：非标准回合、品牌名、模糊导入匹配、自动保留/清理、完整模板。不在T-03顺手决定；数据先保留，不自动GC。

## 7. 需要用户提供

当前返修无新的必需产品资料，默认继续合成fixture；模型、设备路径和单端偏好不重复索要。

需新的安装/依赖或手机测试时，Codex先给出已核查事实和最小授权请求。真实RP、付费API、生产手机升级、强杀共享实例不在默认许可内；只使用已确认的独立副本和新测试namespace，不删除旧证据库。

## 8. 阶段收尾 review

T-00、T-01、T-02A、T-02已verified；T-03为in_progress，P0～P2原型待G1-R1/R2返修复核，G1未放行。隔离桌面已有有限真实持久化/强杀恢复证据，S-A仍在进行；完整有界存储、性能和手机生产使用未验收。
