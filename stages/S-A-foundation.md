# S-A：接入验证与工程地基

状态：in_progress。范围：T-00～T-03（含 T-02A 前置验证）。更新：2026-09-21。

**最新Chat回执：审核1efb334/8fc55e0，接受P4资源停测；另发现P4-R2候选过滤跨提交、P5-R1备份验证未固定导出快照。P5成果保留，但整项仍为implemented_unverified，不升verified。** 详见 `notes/t-03-p4-p5-chat-review.md`；Codex原始证据保留于 `notes/t-03-p4-resource-review.md`。允许在原T-03内按新回执定向修复目录发布放大和两处一致性；不放宽原1x停止条件。T-03仍in_progress；自动维护与手机生产准入不变。本轮Chat独立验证限4文件blob/语法及6个方法级模拟诊断，没有独立执行322项、实际分页恢复或TT实验。

## 1. 阶段目标

证明 Mnemosyne 能在隔离 TT 环境可靠接入生成链路，冻结自己的身份／版本契约，并建立可部署、可备份、可恢复的最小存储地基。

已接受先评估 B 本机版、后完善 A 独立服务器版；B/A 是部署方式，不是阶段编号。采用 B 不代表 A 已交付，正式存储 provider 仍须经过 T-03 选型和恢复验收。

## 2. 任务地图

| Task | 一句话目标 | 当前状态与完成标志 |
| --- | --- | --- |
| T-00 ✅ | 固定真实 TT／柏宝书／测试数据与副 API 基线 | verified；不再猜安装版本、存储模式或输入 |
| T-01 ✅ | 做最小 TT Adapter 探针 | verified；await、payload、取消与 request gate 边界已验证 |
| T-02A ✅ | TT 宿主身份与变更事件验证 | verified；stableId/integrity、Branch、Edit/Delete/Swipe/Regenerate、reopen/rename 边界 |
| T-02 ✅ | 身份、历史版本与上下文的最小可执行契约 | verified；1891043 最终复核关闭 R1～R5，范围限最小参考模型 |
| T-03 ▶ | 可迁移、可恢复的存储地基 | in_progress；G1/P3既有高难项已关闭；本轮修P4-R1资源放大、P4-R2候选快照、P5-R1备份验证；自动维护未完成 |

用户已授权 Chat 核对后发布正式卡并高难前置。旧 `tasks/T-03-storage-foundation.proposal.md` 为 superseded 历史入口；Codex 不修改关卡许可、不自动生成 T-04。G1 正式回执见 `notes/t-03-g1-final-review.md`；维护施工边界见 `notes/t-03-p3-maintenance-review.md`；75abeea首轮问题见 `notes/t-03-p3-integration-review.md`，其临时停在P3要求已被 `notes/t-03-p3-repair-review.md` 的放行覆盖。最新P4/P5回审及定向修复许可见 `notes/t-03-p4-p5-chat-review.md`。均为原任务记录，不是新任务卡。

## 3. 阶段完成线

阶段 A 结束时：隔离 TT 生成前接入可行性已证实；核心身份／版本契约已到可编码且可测试状态；选定 provider 的最小持久化、逻辑导出与空环境恢复经过验证。

本轮 T-03 明确先验收隔离桌面 TT 存储地基：G1 选型/协议审查通过，随后有界清单、完整恢复、规定正确性/1x合成资源冒烟与操作说明闭环。只完成 P0～P2 或 P3 不关闭 T-03；5x/10x 为容量探索，未测规模不得宣称可用。

手机独立准入，安全条件不具备可保留 mobile pending，但不得称为手机生产发布。A 服务器、真实 B→A 迁移尚未完成时单独注明，不以桌面或内存测试替代。

本阶段不要求完成正式剧情导入、自动记忆、完整混合召回、自定义模块或 MCP。第一个可日常使用的档案／原文搜索闭环仍属于阶段 B。

## 4. 已确认输入

历史 T-02A 基线见 `notes/baseline.md`：Windows 10 x64、TT 2.2.0 dev/Canary（git `5e33bf6fead6`）、柏宝书 1.2.9 纯前端本地模式、`system @d0` 注入配置及当时副 API／embedding／rerank 模型。

T-03 P0 新证据：用户更新后的 TT 为 `dev (367b0c7e9410)`，已在 `.t03-local/tt-367b0c7` 独立便携副本/data root/WebView profile 验证 api.db；具体程序哈希、隔离过程与合成库清单见正式 T-03 实施回报。旧5e33记录不代表当前数据库安装，源码支持也不替代实测。

2026-09-20用户提供候选新版 `c90d77d50166`。Chat 对比固定的 `367b0c7...c90d77d` 源码，确认同步维护锁/文件时间处理有变动，但 db.js/types未变、原生Execute仍按namespace分发，没有旧打开代次校验。**新版二进制未在当次review实测，本轮亦未重新验证其他TT版本，不因此更新安装基线或宣布宿主缺口已修复。**

用户已有电脑测试聊天与 ST／TT 导出能力；现用手机不用于首轮破坏性实验。长期 RP 主要使用单台手机，B 版单权威写入/明确交接已确认，不重复询问。

T-02 不沿用柏宝书 Leaf 作为正式对象名；普通派生按 User + Assistant 一轮一份，原文单独归档不意味着 User 单独摘要。

## 5. 已确认方案与本轮落实

- **Head 已接受。** `Branch.head_snapshot_id` → `hs_<UUIDv4>` HistorySnapshot → 有序分块共享 HistoryManifest。固定父快照前缀及锚；父线改旧楼不改变子线，回滚建新快照，正文与记忆版本分离。见 `docs/08-history-snapshot-and-rebuild.md`。
- **正文权威与逐层重建。** 当前分支正文为已发生剧情依据；状态等为派生。匹配适用回合复用，受影响总结/事件/累计状态按轻量 input_refs/coverage 重建，不另建状态权威。
- **T-02 最终 review。** 审核实现 `1891043` 关闭 R1～R5；独立执行范围、Codex 回报与未验证边界见 `notes/t-02-final-review.md`，本轮不重报为新执行结果。
- **存储必须继承的契约。** 06 v0.3 / schema_version=1 / 逻辑包v2及Node参考模型；固定检查点与 current 选择、分支 corrections、R5 根版本不得同时被纠正、derived-only 同线前缀续用、跨Story重绑拒绝及幂等均不可被存储实现削弱。
- **B/A 共用核心。** UUID、来源与正确记忆可迁移；Trivium NodeId/TQL不作领域身份，不因换库全量重摘。当前v2不是未来全产品备份；只验已建模对象及明确版本化的必要恢复材料。
- **T-03 高难前置。** P0必要核对 → P1发布/幂等/恢复协议 → P2极小真实TT故障闭环 → G1 → P3有界清单/完整一致恢复 → P4索引与资源/设备验证 → P5诊断和说明。先证实最危险路径，再扩大CRUD/界面。
- **G1 最终 review 通过。** 审核 `48663c4`；成功关闭 owner 永久失效，关闭失败保留唯一登记/队列，异常 root/payload 不再当作空库。当轮Chat对齐15个文件blob后独立运行84项存储回归及15文件语法检查，全部通过；同一最终19项定向测试在实际旧实现上10通过/9失败。完整150项/23文件语法/diff为Codex结果；原生小验证为已审阅的隔离TT记录，不混称Chat真机执行。详见 `notes/t-03-g1-final-review.md`。
- **B1 阶段性选型。** 现有原语及有限故障证据支持受控隔离路线；不是手机生产provider最终准入。自动外部维护和资源/设备验证仍须分别满足条件。
- **P3维护专项回审。** 接受15b8605的停工理由：原生旧handle在同名库重开后仍可写，不是G1已修复的自有owner再次复活。Codex的211项及旧TT原生记录保留；当轮仅做源码/证据核对，未独立复跑211项、未运行新版TT。
- **P3集成首轮review（75abeea）。** 成员判断和共享依赖遍历曾需按P3-R1/R2返修；314项回归与原生强杀为Codex证据，当轮Chat独立执行两组函数/判定级模拟诊断。历史review不被抹改。
- **P3返修review（4a76c14）。** R1统一当前order与members精确引用，R2采用单次操作有界active/done工作集，缓存命中保留深度检查；拒环、checkpoint及R5继续保留。Chat独立5组测试通过，包括coordinator失败不变性和完整分页恢复。完整319项/旧实现两失败为Codex证据；返修未重跑TT原生。受控高难项通过，允许继续P4/P5。
- **P4/P5本轮回审（8fc55e0）。** 接受Codex保留停止条件与回审的做法；新增P4-R2/P5-R1一致性返修。只读冷开证据、索引独立性和现有诊断说明保留，但不把静态成功等同于并发快照验证通过。允许定向优化库存目录更新，不授权原地覆盖已发布内容或清理历史。

文件优先级：用户最新确认 → 当前正式 task/本阶段导读及最新review → 已验收06/08与T-02/G1最终review → 09协议/07首轮候选/旧proposal及早期架构示意。最新回执是 `notes/t-03-p4-p5-chat-review.md`；`notes/t-03-p3-repair-review.md` 的既有问题关闭和 `notes/t-03-p3-maintenance-review.md` 的隔离条件继续有效。Codex施工回报和旧说明中的完成措辞不得覆盖本次验收状态。

## 6. 当前阻塞、关卡与未决项

**T-02、G1-R1/R2与P3-R1/R2不重新打开。当前处理P4-R1资源放大、P4-R2候选过滤快照一致性、P5-R1固定导出快照核验。** 原生1x未完成，P5整体未verified，自动外部维护隔离仍受阻。

后续执行与许可：

1. **保留成果、先补确定性正确性。** 原322项为Codex回归证据；新增真实coordinator的候选过滤中纠错/编辑、导出前后源库前进，以及恢复目标checkpoint不匹配反例。过滤不得拼接多个当前版本，备份不得把源库继续写入误判为目标损坏。
2. **批准P4-R1定向优化。** 先小规模拆解逻辑put、实际新增页、目录/中间页、native读写/flush和prepare/execute成本；在有界批次内合并目录增量、复用未变不可变路径、减少未发布临时目录结果的无效落盘。不能每轮重建全目录、无限缓存、原地覆盖已发布节点，不能删除任何已保留业务/恢复材料。原生批量API或格式变化需要额外证据和回审。
3. **原完成线不改。** 小样本/旧包兼容/故障不变性通过后保留原322项，再做相同Node 1x与固定TT新run。改变发布/缓冲边界须验证新路径发布前后与丢确认恢复。原30分钟、节点/包/磁盘限制、full持久化不降低；1x未完不跑高成本5x/10x，不关闭S09/T-03。资源分项未测不声称目录是全部延迟来源。
4. **P5局部返修。** 只读诊断、合成命令和文档不重做；备份期望值、导出流和审计必须绑定同一checkpoint，目标新library_id合法。完成P5-R1后可单独复核P5，但不能代替P4资源验收。
5. 自动维护继续返回HOST_MAINTENANCE_UNSUPPORTED。原生代次/租约或可等待维护前排空仅为上游能力建议，不授权改TT或切B2/A。只用已确认独立实例/namespace及合成数据，明确无未经协调同步/归档/库替换；合作式维护须先等待suspend。无法保证隔离时停下相关原生操作。

这不是批准靠用户记得关同步的生产模式。自动维护安全、完整T-03和手机准入仍有门禁；不得用开关假称具备fencing或把既有TT源码核查当成新版本实测。手机缺独立安全条件/许可则保留mobile pending，不替换长期档案。

性能预算尚无用户确认，报告探索性测量；正确性与有界算法不能打折，不测后自设阈值宣布手机达标。MemoryWork的8192状态/131072工作单位/128深度是工程拒绝上限，不是记忆总容量、手机内存或延迟承诺。产品层仍待后续：非标准回合、品牌名、模糊导入匹配、自动保留/清理、完整模板；不在T-03顺手决定，不自动GC。

## 7. 需要用户提供

本次定向返修范围无需用户再确认已有产品规则。P4/P5沿用既有隔离合成样本；模型、设备路径、单端偏好不重复索要。开工先按原约束报告修改范围、实现/测试预计行数及数据兼容边界。

上游接口增强或新受限产品模式仍需另行决定。新安装/依赖、升级隔离版本、原生批量原语或手机测试前，先给出已核查事实和最小授权请求。真实RP、付费API、生产手机升级、强杀共享实例不在默认许可；不删除旧证据库。

## 8. 阶段收尾 review

T-00、T-01、T-02A、T-02已verified；T-03为in_progress，G1和4a76c14的受控P3高难review通过。本轮P4/P5需按最新回执修复三项，P5仍implemented_unverified。固定TT 1x、最终T-03 review、5x/10x、手机和自动维护均未完成；不创建T-04、不冒称生产准入。方法级独立执行摘要见 `evals/t03/p4-p5/chat-review-8fc55e0.json`，不能与Codex全套测试混称。

后续T-04～T-12和B→A工作量的粗估见 `notes/t04-t12-effort-outlook.md`。该文区分累计P3实际代码统计与未来规划区间，不是新任务卡、工期承诺或切换A的授权。
