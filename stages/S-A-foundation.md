# S-A：接入验证与工程地基

状态：verified（受控能力范围）。范围：T-00～T-03，含T-02A。更新：2026-09-21。

**最新回执：`de67c55` 按已记录的用户新范围完成能力验收，T-03升为verified。** `notes/t-03-capability-final-review.md` 是当前收口依据，关闭P4-R2/P5-R1并接受P4-R1的有界目录修复。不再以旧电脑30分钟原生1x未完成阻止后续接口/正确性开发；并不宣称原生1x、容量/延迟、手机或自动维护已经通过。

原task与AGENTS中的in_progress、P3/P4待审、先重测旧机性能等历史状态由此回执覆盖；保留原始实施和实验记录，不把未执行项改为通过。本文件只保留当前导读，逐轮证据见原task和专项review。

## 1. 阶段成果与完成范围

| Task | 当前状态及证据范围 |
| --- | --- |
| T-00 | verified；真实宿主、依赖和数据基线 |
| T-01 | verified；生成前await、注入、取消与request gate |
| T-02A | verified；宿主稳定身份及编辑/删除/swipe/regenerate能力边界 |
| T-02 | verified；1891043最小可执行契约，R1～R5关闭 |
| T-03 | verified；受控TT接口、已验证正确性、持久化/恢复地基；不含生产与性能准入 |

最新用户范围由de67c55记录：判断接口能力、正确性及持久化/恢复可行性，不以本机延迟否决，不要求性能重测先于下一阶段。正常小样本和既有真实故障证据不能外推规模；原S09原生完整1x仍未验证，Node1x完整恢复结果单列。S08中的自动外部维护仍被明确拒绝，不称所有维护场景通过。

本轮Chat读取实现/测试与证据，独立执行7个方法级替身场景；完整333项、63文件语法、双向旧包兼容、Node1x和r3原生强杀是Codex执行证据。未独立重跑完整集成或TT。详见 `evals/t03/p4-repair/chat-review-de67c55.json`。

## 2. 已冻结、不再重复讨论的规则

- Head指向不可变历史快照及分块清单；父线改旧楼不改已存在子线。正文是剧情权威，来源顺序与剧情时间分开。
- Story/Branch不等于聊天文件；新文件续聊需用户明确继承。正式UUID由Mnemosyne生成，宿主stableId及物理NodeId只作映射。
- 正常User+Assistant一轮一份派生；固定checkpoint与current选择分开，纠错按分支传播，R5矛盾导入拒绝。无原文声明不等于损坏引用。
- B本机先行、A独立服务器后续；共享领域规则/ID与逻辑材料，不因迁移重摘正确记忆。当前没有A服务或真实B→A迁移交付。
- 用户长期主要单手机RP，已接受单权威写入与设备交接；不建设离线多主合并。

正式契约仍以 `docs/06-contracts.md`、`docs/08-history-snapshot-and-rebuild.md` 与T-02最终review为准；格式/协议与操作手册见09/10文档。本阶段未实现正式旧剧情导入、自动摘要、完整召回、自定义模块或完整工作台。

## 3. 宿主基线与仍有效的安全门禁

固定隔离测试TT：`367b0c7e9410`，Windows独立便携副本、data root和WebView profile；具体二进制哈希和进程证明见原T-03卡。早期T-02A的5e33只是历史基线。候选c90d77d此前仅源码比较，不能冒充本轮安装/实测版本。

自动外部维护固定 `HOST_MAINTENANCE_UNSUPPORTED`。既有原生句柄按namespace访问重开实例的限制未解决；单端偏好不消除该风险。合作式维护须先suspend并等排空，不能靠JS写前检查或用户记得关闭同步代替原生隔离。

当前只允许既定独立实例、合成namespace和无未经协调同步/归档/换库的开发验证。不改TT、不擅自切B2/A；真实RP、付费API、生产手机及新设备安装/破坏性操作需另行明确许可。保留测试库和备份，不自动GC，不强杀共享实例。

## 4. 未验证项与后续阶段入口

最终TT原生1x未完成的增长后续、该run的索引/冷开/范围读取/完整导出恢复，5x/10x、手机表现及自动维护共存均保留未验证/受限。用户桌面交互影响未量化，不能把超时归咎于用户或简单外推新电脑表现；也不能抹掉实际停止事实。

**下一步是Chat/用户规划T-04，不是自动执行T-04。** 目前没有为下一阶段创建任务卡。非标准回合、旧档模糊匹配、品牌命名、自动保留/清理和完整模板仍留对应后续任务。它们不因地基验收被偷偷定案。

## 5. 测试与成本边界

改动先跑受影响短反例/相关回归，最终候选稳定后再跑完整回归；文档改动不自动全量重跑。长输出落文件，只报告摘要和失败段，避免频繁短轮询与重复读满屏TAP。

当前不再默认启动旧机1x性能重测或5x/10x。新的长TT/破坏性测试须先说明目的、范围、占用、停止条件、是否要求电脑空闲，并取得明确许可；环境失败先做小型诊断，不自动反复消耗长测。已有授权内的低成本工作不需每批重问。详细准则见最新最终review第4节。

## 6. 可追溯资料

- `notes/t-03-capability-final-review.md`：当前验收结论、缩减范围、未测项与测试成本规则。
- `notes/t-03-p4-repair-handoff.md`、`evals/t03/p4-repair/`：333项、旧新包兼容、目录调优、原生r3/r4与交互干扰事实。
- `tasks/T-03-storage-foundation.md`：原任务、S01～S10及全部实施记录；历史状态以最新review覆盖。
- `notes/t-03-g1-final-review.md`、`notes/t-03-p3-repair-review.md`：已关闭的G1和P3问题及各自验证边界。
- `notes/t-03-p3-maintenance-review.md`：外部维护限制及隔离许可。
- `notes/t-03-p4-p5-chat-review.md`：本轮返修的原始反例，不删除历史结论。
- `docs/10-t03-storage-operations.md`：诊断、备份验证与只读回退；不是手机生产运维手册。
- `notes/t04-t12-effort-outlook.md`：未来工作量粗估，不是新任务或工期承诺。
