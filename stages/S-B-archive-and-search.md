# S-B：先把剧情档案拿在自己手里

更新：2026-09-22。状态：in_progress。范围：T-04～T-05。当前正式卡：`tasks/T-04-readonly-import-and-sync.md`；T-05 尚未发布，不自动执行。

**最新回审：`ef67703` 返修及真实演示证据已核对，T-04 仍为 implemented_unverified。** 见 `notes/t-04-repair-review.md`。R1/R2/R3已有对应修复与Codex有限回归；真实面板已读到25条原文和2份有效摘要，但暂停后终态为确认阶段VERSION_CONFLICT，已提交批次数、内容相等与关闭结果仍未核验。当前优先保全原会话并制定一次有界状态核对，不重新创建库或延长演示。用户已暂停，新增原生操作须先明确范围并获许可。T-02/T-03不重开，T-05不启动。

2026-09-22原库核对补证：用户另行授权的一次状态查询已完成（1.363秒），正文25/25、摘要0/2，partial/assets，pending为空；旧owner与本次close确认成功，checkpoint未变化。未续传、未核对逐项内容。仍需剩余2份摘要，不能仅补一个成功标签。证据见任务第12节及evals/t04/native-20260922inspect.json。

## 1. 起点、目标与完成线

S-A 已按 `notes/t-03-capability-final-review.md` 通过受控能力验收。沿用 T-02 身份/版本规则和 T-03 分页提交、持久请求、导出恢复；不重开存储选型、不再要求旧机性能重测作为前置。

| 任务 | 本阶段交付 |
| --- | --- |
| T-04（implemented_unverified，真实落盘核验待完成） | TT 原文 + 柏宝书旧摘要的只读导入；可选薄接物品/地点/生活档案；预览、显式绑定、精确幂等、单向局部同步、删除/歧义人工处理 |
| T-05（后续规划） | 用户手动搜当前分支有效正文、查看来源/旧摘要、必要的档案内回退展示及导出；不实现向量/BM25自动召回 |

Stage-B 的收尾是独立、可恢复、可人工搜索的剧情档案，不是自动摘要/注入上线。T-04 完成后先 Chat review，再发布 T-05。

## 2. 本轮已定，不重新问用户

正式细则与依据见 `docs/11-stage-b-import-policy.md`。

- 导入是迁已有资料，不照搬柏宝书 schema。正文/摘要优先；物品、地点、生活档案可选；眼下局势、人物档案、变量、计划、旧向量/密钥/注入配置不导入。保留正文中的人物事实，不作文本脱意改写。
- 新资料正式 ID 属于 Mnemosyne；有确切映射才自动复用，未知同文也先确认。模糊判断和万能修复器留 T-13/T-14。
- 两个主选项：不继承的新聊天 / 继承已有存档。继承面板明确续聊或分叉及截止；已绑定聊天的普通编辑/重开不重复让用户选择新建。续聊不因换文件新建正文 Head，分叉固定父快照。
- 删除/复杂变更：警告 + 人工核对；受影响同步/记忆使用暂停，旧原文保留；确认后提交。不是让用户负责程序丢数据，也不是实现 AI 自动修复。
- 普通回合只承接一条 User + 紧接一条 Assistant，展示锚为 Assistant 楼层。连续角色、system、开场白等原文照存；不自动改造 LLM 输入、不强制历史全部标准化。T-04 不生成新摘要。
- 手动搜索默认当前 Story/Branch 有效正文；摘要可选辅助。正文原样保存，T-05 用独立规范化投影实现全半角/大小写和空白容错。物品按持有人检索等留实体功能。

## 3. 真正需要开工核对的输入

现有资料已记录原始 JSONL 可导出、TT 隔离环境、柏宝书固定 API 基线；不得反复索要已经提供的信息。

编码助手只需确认实际目标版本/可读入口、获准合成样本里的摘要字段与 coverage、可选 items/scenes/lifeDetails 的实际映射及既有存储接口如何保存最小桥接回执。缺少柏宝书运行样本时，正文路线仍可推进；缺少来源不能伪称全部派生数据完整迁入。

没有新的产品选项需要用户预先做技术选择。实际字段/权限/正确性超出确认范围时才回审。合成小样本可覆盖的事不要求先交出完整真实 RP。

## 4. 安全与权限门禁

TT/柏宝书是只读源；目标仅写新的受控 Mnemosyne 库。Stage-B“同步”不是 TT 数据库同步、整库归档替换或多人/多设备合并。

`HOST_MAINTENANCE_UNSUPPORTED` 保留；合作式维护仍须排空。真实长期档案、生产手机、新安装/共享实例、付费 API、强杀和清理均不由本阶段自动授权。源只读不消除目标维护风险。手机和规模表现仍为未验证，不借阶段推进改成通过。

## 5. 执行与测试预算

先难后易：导入身份/幂等与恢复 → 精确对账和暂停/确认 → 旧资料白名单映射 → 最小导入面板和报告。普通中途提交不是新增审批关卡，无新阻塞继续到 T-04 完成线。

只测任务卡列出的风险；先短反例、相关回归，最终候选稳定后一次全量。文档调整不测，代码未变/无新失败不重复测；日志落文件，只给统计和失败片段。不新增性能比较、1x/5x/10x或反复强杀矩阵。

小型隔离功能演示按既有许可执行；预计超过5分钟的原生循环、新破坏性测试必须事先说明目的/停止条件并获许可。停止新操作不等于取消正在发生的写入。参见 T-03 最终回执第4节。

本次演示已经被用户暂停：先保全原namespace和既有请求；获准后的首步只清点原checkpoint、pending与session进度，不自动执行prepare/execute/recoverPending/confirm/resume。普通面板的“恢复连接/继续”会执行pending，不是只读进度查询。只输出状态、计数与必要匿名标识，不能把pending/lookup返回的私人input写入证据。细则见最新回审第5节。

## 6. 当前证据与后续

首轮 `ba9422c` 的339项Node回归和synthetic源/真实DB恢复证据保留，原问题见 `notes/t-04-chat-review.md`。此后R1/R3/R2修复候选一次345/345由Codex执行；现场null swipe兼容修复另有1项针对性导入/恢复测试通过，最终代码没有再跑346项全量。Chat本轮仅核对固定源码与提交证据，没有独立运行新测试或TT。

实际源读取：用户反馈面板25条原文、2份摘要、11个助手楼层缺摘要。缺摘要属于源coverage报告，不是导入丢档结论。终态见 `evals/t04/native-20260921r1live.json`，为确认阶段VERSION_CONFLICT；与暂停使旧观察代次失效的路径一致，但detail已脱敏为unlisted-error，不能唯一定位触发检查。暂停前未完成的耗时原因仍未知。

收集器5分钟关闭只停止HTTP接收，不自动取消TT导入；演示deadline只在整段动作边界检查，不能称逐批预算已执行。后续如需最小诊断改动，应记录阶段、已提交计数及停止/关闭结果，先给代码量和范围，不开展新长测。

T-04尚未取得本次实际落盘/内容相等完整证据，保持implemented_unverified。无需用户更换大档案重测；先查询原会话，再依据真实状态决定是否需要继续剩余批次。T-05不启动。
