# S-B：先把剧情档案拿在自己手里

更新：2026-09-22。状态：verified（受控档案与手动搜索范围）。范围：T-04～T-05。**T-04 与 T-05 均已通过 Chat 受控能力验收，Stage-B 收口。** T-05 最终回执为 `notes/t-05-final-review.md`，审核实现 `11e5eef`；T05-R1 已关闭。前轮355/355与S08真实TT证据保留，本轮局部返修为按钮反例1/1、受影响工作台10/10；没有重复全量/原生演示，不自动创建或进入T-06/T-07。

**前置验收：`5fe2706` 通过，T-04为verified（受控只读桥接范围）。** 最终回执见 `notes/t-04-final-review.md`，覆盖前轮回审及本阶段历史段落的待审/未完成临时结论。R1/R2/R3和真实原库落盘补证缺口已关闭；原始失败证据与现场过程保留在任务卡和evals。

最终实际状态：25/25正文、2/2摘要complete，正文/候选槽/摘要内容逐项相等、源及原正文Head不变、pending为空，旧owner和最后close均明确确认。状态查询1.363秒，摘要续传及核验43.343秒、一次prepare/execute；没有清库、换库或重导正文，没有重跑全量或性能测试。证据为evals/t04/native-20260922inspect.json与native-20260922resume.json。Chat该轮只审阅源码/回执，没有新运行；不将一次受控核验升级为生产、手机或规模准入。

## 1. 起点、目标与完成线

S-A 已按 `notes/t-03-capability-final-review.md` 通过受控能力验收。沿用 T-02 身份/版本规则和 T-03 分页提交、持久请求、导出恢复；不重开存储选型、不再要求旧机性能重测作为前置。

| 任务 | 本阶段交付 |
| --- | --- |
| T-04（verified，受控范围） | TT 原文 + 柏宝书旧摘要的只读导入；可选薄接物品/地点/生活档案；预览、显式绑定、精确幂等、单向局部同步、删除/歧义人工处理 |
| T-05（implemented_unverified，待T05-R1局部返修） | 用户手动搜当前分支有效正文，显式查看旧快照/旧摘要，分页详情、安全来源跳转或档案内回退、完整库导出；不实现向量/BM25自动召回 |

Stage-B的收尾是独立、可恢复、可人工搜索的剧情档案，不是自动摘要/注入上线。T-05实现后先由Chat验收，S-B才在对应受控范围收口。不能只因任务卡已发布就写阶段完成。

## 2. 本轮已定，不重新问用户

正式细则与依据见 `docs/11-stage-b-import-policy.md`；T-05工程边界与有限验收以新正式卡为准。

- 导入是迁已有资料，不照搬柏宝书schema。正文/摘要优先；物品、地点、生活档案可选；眼下局势、人物档案、变量、计划、旧向量/密钥/注入配置不导入。保留正文中的人物事实，不作文本脱意改写。
- 新资料正式ID属于Mnemosyne；有确切映射才自动复用，未知同文也先确认。模糊判断和万能修复器留T-13/T-14。
- 两个主选项：不继承的新聊天/继承已有存档。继承面板明确续聊或分叉及截止；已绑定聊天的普通编辑/重开不重复让用户选择新建。续聊不因换文件新建正文Head，分叉固定父快照。
- 删除/复杂变更：警告+人工核对；受影响同步/记忆使用暂停，旧原文保留；确认后提交。不是让用户负责程序丢数据，也不是实现AI自动修复。
- 普通回合只承接一条User+紧接一条Assistant，摘要展示锚为Assistant楼层。连续角色/system/开场白等原文照存；T-05原文搜索也不遗漏这些消息。不自动改造LLM输入，不在Stage-B生成新摘要。
- 手动搜索默认单一Story/Branch当前已发布正文；原样精确或NFKC+case folding+空白折叠两模式。先有界分页扫描/临时投影，不新建复杂持久索引。未搜完明确partial/继续，不冒充全库无结果。
- 旧摘要显式开启、独立标记来源及scope；无完整生成证据不升级成当前事实。历史快照显式选择，父线未来/旧swipe不混入默认结果。规范化偏移不能直接用于原文高亮。
- 详情以Mnemosyne的固定原文版本为准，宿主定位不可靠就降级；不靠楼层号猜跳。导出复用完整分页包，清楚标明完整库范围，不能用遗漏bridge材料的小包替代。

## 3. 真正需要开工核对的输入

T-04已核对实际目标版本/可读入口、摘要字段与coverage、可选items/scenes/lifeDetails映射及可恢复桥接回执。不重复要求用户证明能导入，不要求先交完整私人RP。

T-05开工只核对实际库/连接状态、PagedCoordinator只读接口、桥接资料scope/定位、可用公开跳转能力、页面挂载与小型合成fixture。规范化实现来源/兼容性与单批预算由实现者在计划中说明；这不是让用户选择算法。没有新的必须确认的产品取舍。

真实目标、手机、安装或宿主能力超出既有许可才回审。当前宿主缺可靠跳转可以按卡内档案回退验收，不是强制攻克TT内部接口的理由。

## 4. 安全与权限门禁

TT/柏宝书是只读源；目标仅在已获准的独立Mnemosyne库推进。Stage-B“同步”不是TT数据库同步、整库归档替换或多人/多设备合并。

`HOST_MAINTENANCE_UNSUPPORTED`保留；合作式维护仍须排空。真实长期档案、生产手机、新安装/共享实例、付费API、强杀和清理均不由本阶段自动授权。源只读不消除目标维护风险。手机和规模表现仍为未验证，不借阶段推进改成通过。

T-05搜索/详情不提交业务写入，不自动执行pending或恢复导入；open/flush如发生需如实记录。与T-04共享owner时，取消查询不关闭他人的写入连接。不打印正文、摘要、完整pending.input，不向LLM/远程服务发送查询或档案。

## 5. 执行与测试预算

T-04已按导入身份/幂等与恢复→精确对账和暂停/确认→旧资料白名单映射→最小面板完成受控验收。T-05高难优先：W0读入口/owner→W1范围、快照一致性、分页/规范化→W2详情、摘要及可信来源→W3页面、导出与说明。

只测T-05卡中S01～S08有限风险：短反例→受影响分页/页面集成→最终候选一次全量→一次小型隔离TT功能演示。小预算和短fixture验证分页/超限；不增加百万字、1x/5x/10x、随机压力或旧强杀矩阵。文档调整不测，代码未变/无新失败不重复测；日志落文件，只给统计和失败片段，不开子代理。

原生演示默认整轮不超过5分钟；下一页/下一动作开始前检查预算，超时不新发操作，在途调用按原队列排空，单独记录关闭。收集器超时不等于TT任务已停。新长循环/破坏性测试/设备变更需事先说明并获许可，不因“再确认一下”延长占用。

任务内普通中途提交不是新增审批关卡，无新阻塞继续到卡内完成线；新任务仍按用户开工指令执行。T-04的25条原会话已经核验完毕，无须再次导入/续传/测试；临时全局脚本可禁用，原源档和目标库保留。

## 6. 历史证据与当前后续

首轮ba9422c的339项Node回归和synthetic源/真实DB恢复证据保留，原问题见 `notes/t-04-chat-review.md`。R1/R3/R2修复候选一次345/345由Codex执行；null swipe兼容修复另1项针对性导入/恢复通过，最终代码没有再跑346项全量。最终受控验收见 `notes/t-04-final-review.md`，不再以旧临时状态要求重测。

旧演示暂停前延迟的精确原因仍未知；43秒是原会话剩余摘要续传/内容核验，不是完整导入性能基准。旧native-live只在整段边界检查预算的限制已有更正；不得复用为新的逐批预算承诺。

T-05本轮实现与S01～S08证据见T-05任务卡第8节及packages/workbench/README.md。Chat在e12d270首轮回审仅做源码与现有证据审阅，未独立重跑355项或操作TT；本轮发现T05-R1：从历史快照切回当前快照时，旧结果/详情/续页状态未清空。详情见 `notes/t-05-chat-review.md`。先用现有小fixture补真实产品按钮反例，再做相关workbench集成；不自动重跑全量或再次占用TT做完整演示。修复后交T-05/S-B review，不进入T-06/T-07。

## 7. 日常入口与存储边界

B版已经使用TT本机持久数据库，不需要等待独立A服务器。已有apps/tt-import面板及打开工作台的产品接线；临时回环收集器/酒馆助手脚本仅用于验证，不是产品必须常驻的后端。普通入口当前仍偏开发式部署与手填namespace，不能把演示通过称为一键安装发行版或手机生产准入。

修复收口后，建议单独明确小型受控试用入口的安装、既有库选择、重开和备份说明，不再让用户长期依赖临时演示脚本。该建议未创建新任务、不扩大真实数据或维护权限，也不重开T-03。不自动把入口整合、A服务、自动记忆或全面规模验证塞进本次T05-R1返修。
