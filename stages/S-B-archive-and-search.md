# S-B：先把剧情档案拿在自己手里

更新：2026-09-22。状态：in_progress。范围：T-04～T-05。T-04已通过受控能力验收；T-05 尚未发布，由Chat／用户规划，不自动执行。

**最新验收：`5fe2706` 通过，T-04升为verified（受控只读桥接范围）。** 最终回执见 `notes/t-04-final-review.md`，覆盖前轮回审及本阶段历史段落的待审/未完成临时结论。R1/R2/R3和真实原库落盘补证缺口已关闭；原始失败证据与现场过程保留在任务卡和evals。

最终实际状态：25/25正文、2/2摘要complete，正文/候选槽/摘要内容逐项相等、源及原正文Head不变、pending为空，旧owner和最后close均明确确认。状态查询1.363秒，摘要续传及核验43.343秒、一次prepare/execute；没有清库、换库或重导正文，没有重跑全量或性能测试。证据为evals/t04/native-20260922inspect.json与native-20260922resume.json。Chat本轮只审阅源码/回执，没有新运行；不将一次受控核验升级为生产、手机或规模准入。

## 1. 起点、目标与完成线

S-A 已按 `notes/t-03-capability-final-review.md` 通过受控能力验收。沿用 T-02 身份/版本规则和 T-03 分页提交、持久请求、导出恢复；不重开存储选型、不再要求旧机性能重测作为前置。

| 任务 | 本阶段交付 |
| --- | --- |
| T-04（verified，受控范围） | TT 原文 + 柏宝书旧摘要的只读导入；可选薄接物品/地点/生活档案；预览、显式绑定、精确幂等、单向局部同步、删除/歧义人工处理 |
| T-05（后续规划，尚未发布） | 用户手动搜当前分支有效正文、查看来源/旧摘要、必要的档案内回退展示及导出；不实现向量/BM25自动召回 |

Stage-B 的收尾是独立、可恢复、可人工搜索的剧情档案，不是自动摘要/注入上线。T-04已完成Chat review；下一步规划并发布T-05，不因本回执自动开工。

## 2. 本轮已定，不重新问用户

正式细则与依据见 `docs/11-stage-b-import-policy.md`。

- 导入是迁已有资料，不照搬柏宝书 schema。正文/摘要优先；物品、地点、生活档案可选；眼下局势、人物档案、变量、计划、旧向量/密钥/注入配置不导入。保留正文中的人物事实，不作文本脱意改写。
- 新资料正式 ID 属于 Mnemosyne；有确切映射才自动复用，未知同文也先确认。模糊判断和万能修复器留 T-13/T-14。
- 两个主选项：不继承的新聊天 / 继承已有存档。继承面板明确续聊或分叉及截止；已绑定聊天的普通编辑/重开不重复让用户选择新建。续聊不因换文件新建正文 Head，分叉固定父快照。
- 删除/复杂变更：警告 + 人工核对；受影响同步/记忆使用暂停，旧原文保留；确认后提交。不是让用户负责程序丢数据，也不是实现 AI 自动修复。
- 普通回合只承接一条 User + 紧接一条 Assistant，展示锚为 Assistant 楼层。连续角色、system、开场白等原文照存；不自动改造 LLM 输入、不强制历史全部标准化。T-04 不生成新摘要。
- 手动搜索默认当前 Story/Branch 有效正文；摘要可选辅助。正文原样保存，T-05 用独立规范化投影实现全半角/大小写和空白容错。物品按持有人检索等留实体功能。

## 3. 真正需要开工核对的输入

现有资料已记录原始 JSONL 可导出、TT 隔离环境、柏宝书固定 API 基线；不得反复索要已经提供的信息。

T-04已核对实际目标版本/可读入口、摘要字段与coverage、可选items/scenes/lifeDetails的映射以及可恢复桥接回执。缺少柏宝书运行样本时，正文路线仍可推进；缺少来源不能伪称全部派生数据完整迁入。T-05规划只核对与手动搜索/查看有关的下游依赖，不重复要求用户证明能导入。

没有新的T-04产品选项需要用户预先做技术选择。实际字段/权限/正确性超出确认范围时才回审。合成小样本可覆盖的事不要求先交出完整真实 RP。

## 4. 安全与权限门禁

TT/柏宝书是只读源；目标仅写新的受控 Mnemosyne 库。Stage-B“同步”不是 TT 数据库同步、整库归档替换或多人/多设备合并。

`HOST_MAINTENANCE_UNSUPPORTED` 保留；合作式维护仍须排空。真实长期档案、生产手机、新安装/共享实例、付费 API、强杀和清理均不由本阶段自动授权。源只读不消除目标维护风险。手机和规模表现仍为未验证，不借阶段推进改成通过。

## 5. 执行与测试预算

T-04已按导入身份/幂等与恢复 → 精确对账和暂停/确认 → 旧资料白名单映射 → 最小面板完成受控验收。普通中途提交不是新增审批关卡，但新任务仍须用户授权。

只测当前任务卡列出的风险；先短反例、相关回归，最终候选稳定后一次全量。文档调整不测，代码未变/无新失败不重复测；日志落文件，只给统计和失败片段。不新增性能比较、1x/5x/10x或反复强杀矩阵。

小型隔离功能演示按既有许可执行；预计超过5分钟的原生循环、新破坏性测试必须事先说明目的/停止条件并获许可。停止新操作不等于取消正在发生的写入。参见 T-03 最终回执第4节。

本次早期暂停后，已按用户分别授权完成原会话状态查询和剩余摘要续传，现无需再次执行。查询不自动调用prepare/execute；普通面板“恢复连接/继续”会执行pending，不是只读查询。只输出状态、计数与必要匿名标识，不将pending/lookup中的私人input写入证据。临时全局诊断/续传脚本可禁用，原源档和目标库保留，不清理或重导。

## 6. 当前证据与后续

首轮 `ba9422c` 的339项Node回归和synthetic源/真实DB恢复证据保留，原问题见 `notes/t-04-chat-review.md`。此后R1/R3/R2修复候选一次345/345由Codex执行；现场null swipe兼容修复另有1项针对性导入/恢复测试通过，最终代码没有再跑346项全量。Chat本轮仅核对固定源码与提交证据，没有独立运行新测试或TT。

实际源读取先取得25条原文、2份摘要、11个助手楼层缺摘要的报告。缺摘要属于源coverage，不是目标丢档结论。早期演示因暂停后观察代次失效返回VERSION_CONFLICT，具体触发检查仍无唯一证据；随后查询证明正文已完整、摘要尚缺。最终获准续传仅补2份摘要，使用同一固定输入，并逐项核对已发布内容、候选和作用域；pending为空、close成功。完整分页恢复仍引用已有任务内集成证据，不把本次43秒核验说成新的原生全套恢复。

旧native-live只在整段动作边界检查预算，收集器到期不能自动取消TT导入；限制已在bridge README更正。新一次性续传在prepare前检查预算，并记录阶段与关闭结果；不能由此宣称旧harness已全面改造。暂停前延迟原因仍未知，本轮无性能结论，也无新的调优/重测授权。

T-04已verified；S-B未结束。下一步由Chat／用户明确T-05手动正文搜索、来源/旧资料查看和最小工作台的任务范围与有限验收集合。不自动创建或执行T-05，不提前实施T-06/T-07。
