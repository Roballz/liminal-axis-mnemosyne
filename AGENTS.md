# 给本仓库编码助手的工作约束

回复简明、突出结果和真实阻塞。默认单线执行，不开子代理并发；是否切换强推理模型由用户决定，不把模型档位当作任务范围授权。收尾提交并 push，供 Chat 线上 review。

## 当前入口（2026-09-22）

**S-A/T-03及S-B/T-04均已在受控能力范围verified。当前正式任务卡为 `tasks/T-05-manual-search-and-workbench.md`，状态implemented_unverified（S01～S08已完成，待Chat review）。** T-05只做手动正文搜索、旧摘要/来源查看和最小工作台。先读 `stages/S-B-archive-and-search.md`、本卡和11文档相关规则，再按需读现有代码；不反复读取全部历史评审。候选已实现，实施/有限验证记录见本卡第8节；不因待审重跑旧矩阵。

T-03 收口以 `notes/t-03-capability-final-review.md` 为准，覆盖旧 README/roadmap/task 内的 P3/P4待审、先重测旧机性能等临时状态。旧 `T-03-storage-foundation.proposal.md` 不是施工入口。T-04收口以 `notes/t-04-final-review.md` 为准：审核5fe2706，原会话25/25正文、2/2摘要已核验、pending为空、关闭成功；保留原始失败和执行记录，不重复原生导入/续传/全量作为T-05前置。

T-02 的最终契约见06 v0.3、逻辑包v2和08；T-03提供的是受控存储/恢复能力，不是手机生产容量、自动维护共存或 A 服务交付。当前 `HOST_MAINTENANCE_UNSUPPORTED`、真实数据/生产手机权限门禁仍有效。不重开已关闭的地基问题，不自动生成或执行T-06/T-07。

## 需求与规划权

用户最新明确要求、当前阶段政策和任务优先于早期 Blueprint/Leaf/服务器先行示例。U是用户需求，B是建议，E是实验，D是延后；accepted不等于实现verified。用户授权按成熟方案处理的工程细节可以落实，但不得改写故事身份、权限或迁移语义。

任务卡由 Chat/用户创建。Codex 不在开工、执行或收尾生成下一张正式卡；只报告下游依赖。明确要求时也只能生成待审 proposal，不自行启用。

开工先报告实际 commit/分支/工作树、已读当前文件、已知输入、真正缺失项、修改文件、实现和测试分别预计行数、有限验收清单。仓库/环境能读到的信息不得反复询问用户。需要编写代码但尚无用户施工授权时，先给预计代码量并等待授权；文档任务不自动授权写实现。

## 同任务推进与交接

提交检查点不是新增审批关卡。已授权任务内可分批实现/测试/提交，无真实新阻塞、权限需求或明确暂停要求就继续到指定完成线，不每完成一个模块便等待批准。实际执行资源不足时保存可恢复检查点、说明真实原因及下一操作，不能说成 Chat 要求停工。

任务内的架构/数据语义/UX/不可逆迁移问题不得静默改变。需要改已定规则、底层发布协议或权限时带证据和最小方案回审，不扩展成万能解决器。

稳定决定进 docs，阶段范围进 stages，实施记录进当前 task，实验摘要进 evals；不创建不断追加的聊天日志。收尾写实际改动/行数、命令/结果、源码与现场事实的区别、偏差、未验证/阻塞、数据影响、回退与下游依赖。完成仅到 implemented_unverified，Chat 决定 verified。

## 测试成本纪律

1. 只为任务验收清单或真实失败安排测试；短反例/受影响集成先行，最终候选稳定后一次全量。文档/日志改动不测；代码未变、无新失败依据，不因“再确认一下”重跑。
2. 多次全量须说明触发的代码变化和影响面。保留既有正确反例，不靠删测试减少成本；也不为了通过数增加无关变体、工具或随机实验。
3. 长输出落文件，只读摘要和失败段；禁止频繁短轮询、重复全量日志阅读和重新扫描全部历史。强推理用于设计/定位，不持续消耗在等待和搬运日志。
4. T-04/T-05不运行旧机1x/5x/10x、不性能调参、不重复旧强杀矩阵。现有受控环境的短功能验证按当前卡执行；预计超过5分钟的原生循环、新破坏性/强杀/设备变更先说明目的、范围、停止条件并获明确许可。
5. 环境失败先做小诊断；原因未变不重启同一长实验。预算到点停止发起下一页/下一动作，不把取消等待当取消原生执行，不绕过排空/恢复；收集器到时退出不代表TT内部任务已停止。停止及关闭结果分别记录。
6. 分清源码判断、Node/fake、原生TT、手机；没有运行标pending。历史测试数不是本轮执行数，已确认的固定版本证据可引用但不能冒充重跑。

## T-05 当前专属边界

以正式卡W0～W3和S01～S08为边界，先固定范围/版本、游标和有界扫描，再接来源对照/旧摘要、页面和导出。手动按钮发起，先逐页子串匹配和有界临时规范化投影，不新建复杂持久索引。未扫描完必须显示可继续的partial，不把本批无命中称为全库无结果。

默认所选Story/Branch当前已发布正文，User/system也可搜；旧snapshot显式选择并标历史。匹配沿11第7节，两模式、原文不改写；不要把普通lowercase冒充完整case folding，也不要用投影偏移错误高亮原文。旧摘要显式选择、按桥接scope和来源声明展示，不提升为正式memory或自动召回事实。

查询返回/分页/点击详情前复核版本和UI代次；多个分别排队的读取不是一份统一快照，不能重复P4的旧结果放行问题。搜索/详情不调用prepare/execute/recoverPending/resume；pending或paused不能被工作台自动解除。open/flush如发生须如实说明。共用owner时只撤销自己的读取，不越权关闭导入写入连接。

来源跳转必须有当前宿主定位证据，无可靠能力就明确回退到Mnemosyne原文，不猜同号楼层。导出复用固定checkpoint的完整分页包，说明完整库范围，不把遗漏bridge资料的小包叫备份。不为T-05重导此前25条私人样本；演示使用一次小型合成档案，记录实际页面/查询接口断言。

不向LLM/远程服务发送查询或档案，不新增摘要/注入/实体系统、向量/BM25、A服务、GC或内容编辑。T-05完成后只交当前卡与S-B验收，不生成或执行T-06。

## T-04 已验收语义（后续保持）

只读 TT/柏宝书，写独立 Mnemosyne B 本机档案；同步是单向影子对账，不是数据库跨设备同步或 A 服务器。导入正文/旧摘要优先，物品/地点/生活档案仅可选薄旧资料；不导入旧人物档案、局势、变量、计划、向量、密钥或注入配置，不整包复制extra/delta绕白名单。

不调用LLM重摘、不读全部世界书搭摘要管线、不向模型注入、不修复/重排宿主API消息。没有柏宝书不阻止正文路线；缺来源要有声明，不补造来源。最终实体schema和持有人检索不沿用旧插件字段，也不在Stage-B实现。

两个主入口为不继承的新聊天/继承已有存档；内部续聊、分叉、已有映射仍依明确意图和范围处理。已绑定普通重开/编辑不反复建档。只自动认有证据的精确变化，模糊身份交人工。

删除/复杂变化提示人工核对并暂停受影响范围，不自动修剧情或物理清除。忽略警告不解除暂停。程序仍负责不丢数据、不串线、不静默使用已知过期结果；其他故事不得被无关冲突阻塞。

原文逐条保留。普通回合只认一条User紧接一条Assistant，摘要展示锚是Assistant楼层；连续同类/system/开场白/待回复消息照存、不伪造配对、不单独给User摘要。其他分组不在Stage-B实现。

## 共同实现与数据保护

只修改当前任务相关模块，不换栈、不安装无必要服务、不重造既有数据库。运行前核对真实命令，不臆造npm脚本。新持久字段/桥接回执要有版本和可恢复导出，不能藏在临时内存；不反复复制整个state或历史命令充当有界方案。

原始消息与摘要分离；编辑/分支/swipe产生版本或可见性变化，index不是主键。Head指向完整提交的固定快照，各分支历史独立。正文是已发生剧情权威，人物状态为派生；设定和用户大纲不自动等于事件已经发生。

User/Assistant原文完整保存不等于每条都生成摘要；无原文声明不同于损坏引用。派生来源/coverage、current/checkpoint、分支纠错及R5选择一致性必须保留。校验和不替代领域校验，不能静默改selection/删corrections来接受坏包。

隐藏待召回不等于排除；私有/禁止发送内容不得发送。故事/分支/截止/版本有效性过滤不能取消；无rerank分数为null。摘要失效不全局破坏子分支仍使用的旧版本。

源代码仓库不得存真实RP、令牌、生产日志或备份。只用合成/明确获准脱敏数据；真实档案、生产手机、新安装/共享实例、付费API、清理/强杀或批量重写需要相应明确许可。不开自动GC/purge，不按宽泛前缀删除库。

只读源端不等于目标库维护已安全。自动外部维护继续拒绝，合作式维护先suspend并排空；不靠JS检查或用户记得停同步替代原生隔离。不改TT、不擅自切B2/A。

## 宿主与异步边界

除适配层外不导入TT/ST内部模块。接口按固定实际版本取证，不把上游文档当安装版已验证。事件/深拷贝API不是可靠增量日志；停止等待不是取消底层执行。未知写入结果先恢复原操作，不能换ID盲重试。

异步结果应用前复核run、宿主绑定/观察代次、Head及相关记忆/策略版本。一个内容类别只允许一个主动注入者；Stage-B本身不注入。宿主文件、stableId、物理NodeId不替代正式身份。
