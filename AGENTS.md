# 给本仓库编码助手的工作约束

当前是Codex环境，回复简单且突出重点。每次任务收尾后须提交并push到仓库，供Chat线上review。

## 启动时读取

先读取 README.md、对应 `stages/` 阶段导读、用户指定的当前 task；再按 task 读取架构、决策和契约相关章节。不要每次盲读全部历史聊天；报告使用的 commit 和文档版本。

T-02 已 verified，审核实现1891043，最终证据及未验证边界见 `notes/t-02-final-review.md`。**当前正式任务为 `tasks/T-03-storage-foundation.md`，in_progress；48663c4 已通过 G1，关闭 G1-R1/R2，当前P3有界基元/v1完整恢复增量与维护阻塞见 `notes/t-03-p3-handoff.md`；P3未完成，不直接进入P4/P5。开工同时读 `notes/t-03-g1-final-review.md` 第5节，同时读 `notes/t-03-p3-maintenance-review.md`：自动外部维护门禁保留，其余P3获准在受控隔离环境继续；现有分页基元尚未接入普通领域编译/发布，不将其误记为S05完成。** 旧 `T-03-storage-foundation.proposal.md` 已 superseded，不是并行施工入口。不自动创建T-04。G1放行的是B1路线的下一增量，不是手机/生产存储最终准入；历史实施回报中的“G1待审”不覆盖最新回执。

用户最新明确确认、当前决策记录及 task 优先于早期建议/示例；`docs/06-contracts.md` v0.3 / 逻辑包v2已在T-02最小参考模型范围验收。发现冲突先报告，不拿旧 Leaf、宿主 generation ID 或服务器先行示意覆盖新确认。

## 需求与决策

U 是用户明确需求；B 是建议基线；E 是待实验；D 是延后。B 不是用户已经逐项批准，E/D 不能擅自固化成不可逆设计。任务受阻时给出具体证据与最小改动方案，不编造宿主 API 或测试结果。accepted 是设计确认，不等于实现 verified。

## Chat ↔ Codex 交接

仓库是 Chat、Codex 与用户之间的工程共享基准，不依赖聊天上下文互通。每个阶段先读阶段导读；每个实现增量只执行用户指定的 task。

开工先报告：当前 commit/分支/工作树、已读文件、任务范围、已知输入、真正缺失项、预计修改文件、实现和测试各自预计行数、验收方式。仓库已记录的信息不得重复索要。

收尾实施回报写回本 task，包含实际改动、环境与命令、测试结果与证据、宿主/接口事实、偏差、未验证项/阻塞、数据或契约影响、回退、下一任务依赖及需 Chat/用户决定的问题。观察事实与设计决定分开；影响架构、数据语义、UX 或不可逆迁移的事项不得静默定案。

**提交检查点不等于新增审批关卡。** “每次任务收尾提交并push”“每次只实现一个可验收增量”用于限定范围和保存证据，不表示已授权的同一任务每完成一个模块都必须停止等Chat重新批准。允许在任务内分批实现、测试和提交；没有新的阻塞、权限需求或明确暂停指令时，继续到用户指定的完成线或任务已有的强制关卡。因实际执行资源/上下文限制不能继续时，保存可恢复检查点，说明真实原因及下一操作；不得把此类中断说成Chat要求暂停，也不得把未完成阶段称为已验收。

**P3 当前续工边界（2026-09-20）：** `4caa326` 是有界基元和现有intent恢复的进度检查点，不是新增停工关卡或P3验收通过。剩余普通业务有界编译、分页发布/检查点和规模化完整恢复仍在原许可内，按 `notes/t-03-p3-handoff.md` 第5节继续集成。自动外部维护受阻只冻结该路径及生产准入，不冻结具备既定隔离条件的其他P3开发。新出现的正确性/宿主能力缺口仍须带证据回审；不为赶进度绕过门禁或静默更改领域语义。受控P3集成完成后更新高难项交接供review，P4/P5不以零散基元测试通过为由提前启动。

**任务规划权属于 Chat/用户。** Codex 不在开工、执行或收尾时自动创建下一张正式任务卡；只报告下一任务依赖/建议验证点。明确要求时，也只能在当前任务完成后产出 proposal 候选，等待 review。用户授权 Chat 提前创建的预备卡同样不是自动开工许可。

不要建立不断追加的 Chat/Codex 对话日志。稳定决定进架构/决策；阶段目标与待定项进阶段导读；实现证据留 task；实验数据进 evals/或脱敏附件。

## 实现纪律

每次只实现指定任务或一个可验收增量。先列改动与验收条件；不得顺手换技术栈、重写无关模块、安装不必要的大型服务。运行命令前检查真实工程配置，不臆造 npm 命令；已有探针测试使用 Node 原生命令，新测试入口依 task 建立。

所有测试/构建必须真实执行；未执行标 pending。mock 不代替 TT 手机联调，小样本不代替大规模性能，内存参考模型不代替持久化/崩溃恢复。示例 JSON 不是生产校验器。

T-03先攻发布/幂等/故障恢复，再扩建有界清单、完整备份和常规界面。不得把T-02整份state/完整command历史反复复制的oracle原样当作手机生产存储。G1已放行B1的P3实施；若实际原语不足以完成维护隔离/恢复或有界性，先给证据回审，不擅自切B2/A或降低标准。P3完成先更新高难项交接，P4/P5按原卡安排，整个T-03不能提前升为verified。

## 数据保护

原始消息与派生摘要分离，不覆盖唯一原文。编辑/分支/swipe 是新版本或有效性变化；消息 index 不作主键。Head 指向完整提交的不可变快照；各分支固定历史独立。数据格式变化需迁移、导出与回退，不在唯一真实存档上测试。

源码仓库不得包含真实 RP、令牌、生产日志或备份。删除数据、改仓库可见性、覆盖存档和批量重写需明确授权。B 单权威写入不等于免除异步写入协调或崩溃恢复。

默认纯虚构fixture和隔离测试库。强杀/清理前核对精确测试实例与影响范围，不终止含真实数据的共享TT、不填满生产磁盘；手机须有独立安全条件与许可。备份只承诺实际建模对象及已说明的恢复材料，不伪称已覆盖全部未来模块。

## 记忆语义

已发生剧情以该分支有效正文为权威；人物状态等为派生，不另立状态权威。普通 User + Assistant 一轮一份派生记忆；单独保存 User 原文不代表给它单独摘要。

隐藏待召回不等于排除，地点不是激活硬门槛，重点详情不得静默压缩。召回过滤故事/分支/截止点/版本适用性。V1 为上帝视角，不按单个角色是否知情屏蔽普通剧情；用户明确禁止发送或排除的内容仍不能进入提示词。无 rerank 时分数为 null，不用相似度冒充。

复用适用记忆；变化回合及包含它的总结/事件按轻量来源与 coverage 逐层重建。失效相对于分支，不全局破坏共享旧对象。未知回合/身份不猜测，不丢原文；derived-only 与损坏悬空引用分开。

持久化须保留view版本/selections/corrections的一致性、current/checkpoint区别和R5根选择校验；合法checksum不能替代领域校验。恢复不能靠静默选新版本或删除纠错关系解除矛盾。

## 宿主与网络

除适配层外不导入 TT/ST 内部模块。远程 API、生成拦截与最终注入位置须用目标安装版本验证。异步结果应用前复核 run、绑定/观察代次、历史 Head 以及相关记忆/策略版本；不得假定 TT 提供 Mnemosyne 的正式身份。

清理旧注入槽，一个内容类别只允许一个活跃注入者。数据库能力按真实版本取证，不把上游源码支持当作用户设备已支持，不把多个 API 调用当成已存在的通用事务。停止等待不等于原生写入已取消；取消/丢确认后的重试必须先判定原操作状态。

## 收尾

报告真实改动、实际代码量、测试命令/结果、未验证项、回退和下一任务依赖；同步 task、CHANGELOG 和受影响文档。实现完成只到 implemented_unverified，Chat review 决定 verified。部分关卡完成不代表总任务完成。不要把计划写成已支持。
