# 给本仓库编码助手的工作约束

## 启动时读取

先读取 README.md、docs/01-architecture.md 第 0～3 节、docs/03-decisions-and-open-questions.md，再读取本次 task 和相关章节。不要每次盲读全部历史聊天；以当前仓库提交的文档与测试为准。报告所使用的 commit 和文档版本。

## 需求与决策

U 是用户明确需求；B 是建议基线；E 是待实验；D 是延后。B 不是用户已经逐项批准，E/D 不能擅自固化成不可逆设计。任务受阻时给出具体证据与最小改动方案，不编造宿主 API 或测试结果。

## Chat ↔ Codex 交接

仓库是 Chat、Codex 与用户之间的唯一工程共享基准，不依赖聊天上下文互通。每个阶段开始前先读对应 `stages/` 阶段导读；每个实现增量只执行对应 `tasks/T-XX...`。

开工时先在回复中报告：当前 commit、已读文件、任务范围、已知输入、真正缺失项、预计修改文件和验收方式。仓库已记录的信息不得重复向用户索要。

收尾时必须把实施回报写回本 task，至少包含：实际改动、环境与命令、测试结果与证据、观察到的宿主／接口事实、与任务卡的偏差、未验证项／阻塞、数据或契约影响、回退方式、下一任务依赖，以及需要 Chat／用户决定的问题。观察事实与设计决定分开记录；影响架构、数据语义、用户体验或不可逆迁移的事项不得由 Codex 静默定案。

**任务规划权默认属于 Chat／用户。** Codex 不得在当前 task 开始时、执行中或收尾时自动创建下一张 `T-XX` 正式任务卡；只报告“下一任务依赖／建议验证点”。只有收到明确要求时，才可在当前 task 已完成并写完实施回报后生成下一任务的候选草案，并标为 `proposal`，等待 Chat review 后才能成为 `planned`。

不要建立不断追加的“Chat/Codex 对话日志”。稳定决定进架构／决策文档；阶段目标与待定项进 `stages/`；实现证据和问题留在对应 task；实验数据进 `evals/` 或 task 附件。

## 实现纪律

每次只实现用户指定的一个任务或一个可验收增量。先列出将改动的文件与验收条件；不得顺手更换技术栈、重写不相关模块、安装未必要的大型服务。首次运行命令前确认真实 package.json／环境；当前文档包尚无 npm 测试命令。

所有测试与构建命令必须真实执行；未执行项标为未验证。不能用 mock 通过代替 TT Android 联调通过，不能用小样本通过代替亿字性能通过。示例 JSON 不是完整生产校验器。

## 数据保护

原始消息与派生摘要分离；不覆盖唯一原文。用户修改、分支和 swipe 是新版本／有效性变化。引擎不依赖 TT mesid 作为主键。数据格式变化必须有迁移、导出和回退方案。不要直接在真实用户唯一存档上测试。

源码仓库不得包含真实 RP 内容、令牌、生产日志或备份。删除数据、变更远程仓库可见性、覆盖存档和批量重写必须得到明确授权。

## 记忆语义

隐藏待召回不等于排除；地点不是激活硬门槛；重点详情不得静默压成摘要。所有检索分支都过滤故事、分支、截止点和可见性。作者秘密不进入普通正文提示词。无 rerank 时分数为 null，不可用相似度冒充。

## 宿主与网络

除适配层外不导入 TT/ST 内部模块。远程 API、生成拦截、system/user @d0 必须用目标安装版本验证。异步响应应用前必须复核本轮请求身份、当前聊天身份和当前 head；这些正式字段如何命名由契约任务冻结，不能把宿主没有提供的 generation ID／head revision 当作既成事实。清理旧注入槽；一个内容类别只允许一个活跃注入者。

## 收尾

报告：改动摘要、实际测试命令与结果、未验证内容、回退方法、下一任务依赖。同步更新 task 状态、CHANGELOG 和受影响文档。不要把“计划实现”写成“已经支持”。
