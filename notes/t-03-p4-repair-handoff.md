# T-03 P4-R1/R2 与 P5-R1 返修回报

施工基线：main@fc3264b，依据 `t-03-p4-p5-chat-review.md`。本轮为原T-03返修；实现状态 `implemented_unverified`，不创建或执行T-04。

## 范围与实现

- 候选过滤复核前后library、checkpoint、Story/Branch、Head和view；默认返回`{status, results, source}`。漂移返回behind且无旧结果；search传递该状态，rebuild在完成前复核并保留旧source依据。未来注入端仍须在应用异步结果时再次复核。
- `exportSnapshot(branch)`在同一次coordinator队列边界捕获诊断和固定目录导出流，不从队列内调用另一个handle。备份验证比较这个快照的checkpoint、operation数和分支信息，执行目标审计并再次检查目标未变；后续源写入不参与比较。允许恢复生成新的library_id。finally关闭目标，关闭错误作为`close_error`附加，不覆盖主核验错误。
- 物理目录最终每批至多1024个不同引用；`mapSetMany`按排序批次触及必要AVL路径，复用未变不可变子树。512条已登记引用缓存仅在当前TrackedPages实例有效，恢复/维护重开重建。业务页仍先做物理地址碰撞校验，未删除任何已落盘页。
- 临时候选目录工作集限16,384页、16MiB；超限明确RESOURCE_LIMIT。只把最终批次根可达的候选目录页落盘，从未发布的临时计算结果可丢弃。导入使用同一有界批次，旧目录页格式和业务control引用不变。
- 材料持久化、单root发布、发布持久化、确认及未知结果恢复顺序保留。目录实际写入新增before/after故障点。聚合计量不存payload/逐调用日志，分别统计业务put/已存在/新增、目录候选/落盘、IO get/put/flush和prepare/execute/compile/发布目录耗时；嵌套耗时不能直接相加。

## 已执行验证

Windows x64 / Node v24.19.0；固定TT 367b0c7e9410，exe SHA256 `11a9bc110da5dc634ff8c0b7b8fe244110c360af46693e50de67968cb811f2c4`。

1. 首批4个真实coordinator/恢复反例在旧实现全部失败：过滤误报ready、rebuild误报ready、导出后源前进误报audit mismatch、错误checkpoint被放行；修复后4/4通过。详细工具执行摘要见 `../evals/t03/p4-repair/before-summary.json`，不是伪造TAP。
2. 最终完整回归333/333，原322项保留，新11项。覆盖真实纠错/编辑/view交错、源导出前/恢复期间追加、目标不同library正例、错误checkpoint、关闭错误、目录写入两侧失败、旧根及AVL结构。原候选测试只适配新的结果对象，保留原领域排除断言。
3. 从fc3264b实际导出旧源码，旧包→新恢复、新包→旧恢复均通过；逻辑内容、checkpoint及幂等回执一致。旧源码保留在隔离环境，小包通过流传递，未迁移真实数据。
4. 当前63个JS文件语法通过。首轮41项定向回归通过；最终完整TAP覆盖最后实现。
5. 最终1024批次固定TT新run `20260921r3`：发布前强杀后为prepared，发布后丢确认强杀后为published；generated IDs、独立边界结果和重试幂等均通过。r2为128批次的中间证据。完成后的退出另列为清理，不作故障证据。

## 分项计量及规模结果

固定小集fixture(8)一个history prepare/execute：原目录prepare约12,543ms，最终1024批次约1,625ms；目录候选17,045→2,720，目录落盘6,908→2,349，物理节点8,472→3,912。业务generated IDs仍用正常UUID，AVL布局计数可有小幅波动；这些是一次小集对照，不是1x成绩或手机预算。

中间128批次Node 1x完整通过，workload150,926ms、恢复145,396ms，源281,940节点（陈旧目录217,119），包20,959,225 bytes，峰值RSS795,111,424 bytes。分项显示仍有249,529次目录落盘，因此在同一批准范围内调整为1024引用批次；临时候选硬上限和原资源停止条件不变。中间结果与包保留，未据此提前跑TT 1x。最终批次重新执行333项和双向兼容，均通过。

最终1024批次Node 1x完整通过：workload97,266ms、冷重开44.64ms/20次provider读取、导出8,078ms、恢复138,766ms。源187,816节点（业务32,341、活动目录32,341、陈旧目录123,133）；恢复64,819节点（陈旧目录136）。包20,909,960 bytes，峰值RSS617,533,440 bytes。与原333,608节点相比下降约43.7%；Node计量provider不是固定TT或手机结论。

固定TT最终1x：run `20260921r4` 触发原 `P4 native elapsed-time safety stop`，未完成。已上报增长4/8/12/16/20次的累计耗时分别为124,728.5 / 344,508.7 / 633,063 / 959,822.4 / 1,348,946.4ms，物理节点分别为7,644 / 20,156 / 36,137 / 54,080 / 74,952。下一次进度检查在时间断言处停止，未发送该检查的耗时/节点，不补造停止点数据。完整增长、索引、冷重开、导出及恢复未完成，本轮不能关闭S09或T-03。

20次进度的分项：native get 217,467次/345,367.6ms，put 74,992次/932,616.9ms，flush 82次/26,363ms；目录候选74,648、落盘59,423，prepare编译245,988.2ms。嵌套计量不能相加。与旧run同为8次时相比，本轮累计时间约少68.0%、节点约少50.4%；这是不同run的观察值，不是严格隔离的因果对照。

用户在结果返回前说明期间曾改变TT窗口大小、操作其他应用及改变窗口层级。未记录交互时刻、遮挡/最小化状态或WebView调度，无法量化影响；本次有桌面交互干扰，不能把超时单独归因于目录算法，也不能把它当作无干扰性能基准。原限制下未完成是本次运行事实，是否需要受控重测交Chat/用户决定，本轮不擅自追加昂贵重跑。

隔离进程树10:27:41Z采样工作集1,039,417,344 bytes；该值不是Node RSS或峰值。data root由160,045,598增至240,344,103 bytes（增加80,298,505），结束磁盘余量365,676,269,568 bytes。停止后核对唯一PID12504、精确路径/二进制哈希及新错误证据；正常关闭3秒仍未退出后强制结束，仅为资源停止清理，不是故障恢复验收。原始事件与退出记录见 `../evals/t03/p4-repair/20260921r4-p4-resource.json` 及同名前缀exit文件；所有库保留。

限制保持：Node heap 1536MiB，RSS 1400MiB，包512MiB，物理节点100万，单场景30分钟，磁盘余量20GiB。5x/10x只在1x完成且资源允许时探索。

## 环境异常、回退和边界

首次原生run `20260921r1` 的collector受沙箱EPERM阻止写证据，未取得可采信边界；此run无效、库保留，退出仅是测试清理。获准写入后以新r2重测。r2发布前强杀曾因自动审批上游断连未执行，用户续工后重新执行；不是宿主/算法失败。

回退停用新命令和索引入口、保留所有源库/暂存库/证据；双向小包兼容支持旧分页读者，不代表任何格式迁移。自动维护仍`HOST_MAINTENANCE_UNSUPPORTED`，手机pending，无TT修改、provider切换、自动GC或原地覆盖。

Chat需复核两处一致性闭环、有界目录算法及实际规模记录，并决定桌面交互干扰下的TT受控重测安排。本回报为原任务返修回审，不是整个T-03最终验收；P4-R1/S09完成线仍未过，不自行关闭返修项或升verified。5x/10x未执行。
