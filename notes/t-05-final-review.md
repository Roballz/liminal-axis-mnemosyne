# T-05 最终 Chat review（Stage-B 收口）

日期：2026-09-22。审核实现：`11e5eef10ab46e4f3c69851943abba8ec9319527`；首轮审核实现：`e12d270`；首轮回执：`notes/t-05-chat-review.md`。

## 1. 结论

**T-05 在当前受控手动档案/搜索范围通过，状态升为 verified；T05-R1 关闭。Stage-B（T-04～T-05）按受控范围收口。**

本回执覆盖首轮回审中的 T05-R1 待修状态，但保留历史失败与证据。T-02/T-03/T-04 不重开；本回执不创建或执行 T-06/T-07。

本轮 Chat 核对 `11e5eef` 相对 `4d53a51` 的单提交改动、产品页面状态路径、新增按钮回归及任务/阶段回报。**没有执行新的 Node 测试、TT、手机或私人档案操作。** 前轮 355/355 与 S08 原生 TT 演示继续作为已提交证据引用，本轮不冒充重复执行。

## 2. T05-R1 关闭依据

返修仅修改工作台页面内存状态及必要测试，没有存储、导入、规范化或发布协议变化。

- 新增 `clearPage()`，统一清除旧 `results`、`detail` 和 `lastPage`。
- “选择档案/当前快照”在第一次异步读取前即清理旧显示、取消旧 service 会话、清空旧选择，再进入新快照选择；选择失败也不保留旧页面内容。
- “更早的历史快照”采用相同的先清理/取消规则，同时先保留已选快照的 previous 身份，避免取消旧 service 后丢失历史链入口。
- 既有 UI generation/token 校验保留，并显式传入选择流程；较迟返回的旧详情即使业务读取已成功，也不能重新绘制到新范围。
- 旧游标在切换后失效，重新选择当前快照后新查询使用当前 Head；Head/checkpoint 不因这些页面动作改变。

新增产品按钮回归覆盖：H1 历史搜索/详情 → 切回 H2 前立即清空 → 延迟旧详情返回不重绘 → H2 选择成功 → 旧游标拒绝 → H2 新搜索正确 → 选择失败仍保持清空。Codex 记录定向 1/1、受影响工作台集合 10/10 通过。

这正面覆盖首轮回审提出的问题；未发现需要扩大到 readView、分页存储或导入层的新缺陷。

## 3. 继承的 T-05 证据

前轮证据继续有效：

- 最终候选一次全量 355/355，通过日志为 `evals/t05/full-tests.tap`。
- S08 在真实 TT 环境、既有合成档案上通过实际产品页面处理函数完成分页搜索、详情、旧摘要、档案回退及完整包导出；查询业务写入为 0，共享 owner 保持可用，最终 close 明确确认。
- 完整分页包恢复、正文/候选空位/摘要/桥接回执查询属于 S07 的 Node/FakeIO 证据；不把它说成 S08 又做了一次原生空库恢复。
- 工作台普通产品入口已从 T-04 导入面板接入；临时回环 collector/酒馆助手脚本只用于现场验收，不是产品后端依赖。

R1 是页面状态局部修复。按既定测试成本纪律，1/1 定向 + 10/10 受影响集合已经覆盖改动面，**不要求为了收口再次运行 355 项全量或 TT 原生演示。**

## 4. Stage-B 收口能力

Stage-B 现在具备受控的完整手动档案闭环：

1. TT 原文和柏宝书已有摘要可只读迁入 Mnemosyne B 本机持久库；
2. 精确绑定、续聊/分叉、局部同步与复杂删除人工确认遵守 T-04 已验收规则；
3. 用户可手动搜索所选 Story/Branch 当前正文，显式查看历史快照和旧摘要；
4. 查询固定版本、有界分页，旧结果/迟到结果不能跨快照冒充当前结果；
5. 可查看固定原文与邻接；宿主定位无可靠公开能力时明确回退 Mnemosyne 档案；
6. 可导出固定 checkpoint 的完整分页恢复包。

因此 Stage-B 达到“自己的可恢复档案 + 人工搜索入口”的目标。

## 5. 仍未宣称的范围

本次收口不是生产/手机全面准入。以下保持未验证或后续范围：

- `HOST_MAINTENANCE_UNSUPPORTED` 仍有效，合作式维护必须排空；
- 手机容量/延迟、长期真实大档案规模、旧机 1x/5x/10x 不因本验收变成通过；
- 当前没有经核验的 TT 宿主直接跳楼能力，来源按钮允许档案回退；
- 工作台完整包下载当前有 16 MiB 上限，超限明确拒绝，未宣称大库下载能力；
- 正常入口仍偏开发式部署/库选择，不等于一键安装发行版；
- T-06 向量/BM25 自动召回、T-07 新摘要、T-08 注入编译、T-09 自动生成前闭环均未开始。

若用户希望先日常使用现有手动搜索能力，可在后续单独规划一个很薄的受控试用封装（安装/固定入口/既有库选择/重开/备份说明），不需要新存储后端，也不必等待 A 服务器；该事项不由本回执自动施工。
