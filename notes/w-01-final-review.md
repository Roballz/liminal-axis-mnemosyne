# W-01 最终回执：加入事件按钮布局与成功提示

日期：2026-09-24。状态：**implemented_unverified**。本节为最新回执，下方保留历史证据。

- 开工分支 `work/daily-memory-mvp-baibai`，HEAD `4f190d2faaf87e93946ac2f9cf1935d042eb0616`；工作树干净，fetch 与远端一致。固定柏宝书基线再次核对为 `393873acd27906a09308ae65fe7e636a3d3941ab`。沿用 W-01 用户施工授权与 AGENTS 单线要求，无子代理、无新任务卡。
- 提示词核对：`prepareEventRewrite → requestEventOverview → buildEventInstruction` 读取已保存的 `apiSettings.prompts.eventOverview`（空值用内置），再附固定输出协议、重写要求及事件材料。本次无需改请求逻辑；使用说明补上“重写概要”也从下一次请求读取设置。已有合成测试仍覆盖自定义提示词与改写要求实际进入请求。
- 图一问题来自 `.mn-menu` 虽设 gap，却没有启用 flex/grid；其固定宽度/内边距也不适合嵌入该表单。楼层动作使用独立双列 grid，两按钮等宽、间距12px、上间距20px、高度至少46px，居中显示。立即更新保持主色，仅入库与取消保持次要配色，其他菜单不变。
- 图二成功提示统一为“已加入事件链”，省去“已在事件链中”和未调用模型的说明。关联去重、仅入库无API、立即更新需审阅等行为保持。
- 实现源码 +13 / −7行；现有浏览器检查与截图记录 +7 / −1行。未新增单元测试或无关场景。实现估计约20行，实际更少；文档、构建和截图不计入源码行数。
- 本地实现 `f95ba2add1d3baa4950e05bd9cba79443d9495d8`；GitHub 实现 `e18f9d540d8d59a0d6398845126c435ea792bf31`；共同树 `2d2a00918a16bdb616271338e98f5e2e5282e5a1`，9个blob与整树核对。本回执另作纯文档提交后，一起非强制更新原分支。
- 最终一次 `npm test`：23文件367/367通过，既有脚本810/12/53断言通过（`/tmp/w01-join-test.log`）。`npx vue-tsc --noEmit` 通过（`/tmp/w01-join-types.log`）；`npm run build` 通过，CSS105.54kB / gzip16.07kB，JS756.95kB / gzip263.48kB（`/tmp/w01-join-build.log`），保留既有单包>500kB提示。`git diff --check` 通过。
- 复用现有浏览器烟测：`MN_CHROMIUM_PATH=/tmp/w01-join-chromium/chromium MN_TEST_FONT_CSS=/tmp/w01-join-tools/node_modules/@fontsource/noto-sans-sc/400.css node scripts/daily-manual-ui-smoke.mjs` 通过，日志 `/tmp/w01-join-ui.log`。Chromium153、390×844，检查现有加入动作/提示断言、无额外stub调用，并人工查看暗色合成截图：[双按钮布局](w-01-ui/event-join-actions.png)、[加入成功](w-01-ui/event-joined-feedback.png)。无pageerror，未调用真实模型或使用真实聊天/用户截图做夹具。临时浏览器/字体已重建在/tmp，项目依赖未变。
- 无库格式、数据、迁移或请求语义变化。TT手机尚未实测；更新并重载扩展后，楼层卡片“加入已有链”选中一条链，确认双按钮间距，点仅入库检查新提示即可。可回退至开工提交 `4f190d2` 恢复旧布局和文案，无需回退数据。继续交 Chat review 与 TT 手机实测，状态仅 implemented_unverified。

---

# W-01 历史回执：事件返回审阅与按要求重写概要

日期：2026-09-24。状态：**implemented_unverified**。本节为最新回执；下方是历史记录。

## 版本、授权与范围

- 开工分支 `work/daily-memory-mvp-baibai`，HEAD `fdd51ad4727bf27e6f822f8fda330c7dd6d26ad2`，工作树干净；fetch 后与远端相同。固定柏宝书基线仍为 `393873acd27906a09308ae65fe7e636a3d3941ab`。已读 AGENTS、W-01、13/14/15/stage，单线实施，无子代理。
- 用户先要求坏 JSON/空返回也可编辑审阅、确认后才写入；随后追加长按归档菜单下“重写概要”和改写要求输入。本轮一并完成，不新增审批或正式任务卡。
- 本地测试实现提交 `18723d2f649d923303ea590ab0053c3bd907b779`；GitHub 实现提交 `8e24092f34ea26f044b81805a5e02691125ac377`，共同文件树 `7e7c845b64dfcc3f751d63fd998af712e92d9fd8`。19个文件 blob 与整树逐项核对；本回执另作纯文档提交，随后一起非强制更新原分支。
- 相对开工，源码实现 **+687 / −137行**，测试/浏览器夹具 **+449 / −18行**；不含文档、截图及构建产物。两次实现估计合计510～720行，测试估计230～370行；测试多79行，来自真实页面的有效/空/截断返回、修正、取消、跨卡片操作锁及归档重写完整路径。

## 实现结果

1. `manual-events.ts` 将楼层新建和立即更新分成生成草稿、确认提交两步。返回内容不先做领域解析，正确/错误/空正文都可进入审阅；新链、概要、本次新增关联和追加进展只在确认后同事务发布。仅入库仍直接关联且不调用模型。
2. `EventResponseReview.vue` 复用现有顶层弹窗与可拉伸文本框，展示可编辑 JSON、格式说明、可展开的原始正文和原始校验结果；取消保持次要配色，底部确认始终可见。点击外部或 Esc 不丢草稿，Tab 限定在窗口内。错误时确认不可用；确认处理再次校验，双击/重复提交不能建第二条链。
3. `event-response.ts` 给出空正文、语法错误、重复字段、最外层类型、标题/状态/关键词/概要/进展具体要求；保留500字限制与严格 JSON 边界。原生解析器有位置时给行列，疑似截断明确是推断。修正只在本地执行，不重调模型。导入 JSON 解析器不变。
4. `api/client.ts` 仅人工审阅调用启用 `reviewResponse`，保留可用输出正文（含空串及首尾空白）。普通请求维持空响应拒绝；HTTP/API 错误仍报错，不伪装生成成功。同时补收 SSE 最后一行无换行的数据，避免漏掉最后一个有效 delta。
5. 长按事件折叠卡片，在“归档／取消归档”下增加“重写概要”。先输入改写要求，再确认一次摘要API请求；发送当前概要、全部有效成员正文/摘要及要求，复用自定义事件提示词和请求预算。已归档、已全部整理的链均可重写；无有效成员时发送前提示。
6. 重写返回也进入上述审阅窗口。确认后更新原链，归档标记与历史版本保留；纯措辞重写不为已整理内容新增进展。人工确认的标题、状态、关键词和概要按草稿保存，`progress` 仅为未整理成员追加。模型不会自行另建链。
7. 确认前复核宿主、观察代次、活动库、分支/Head/视图和合法来源；写事务内再次检查守卫。切聊天/卸载丢弃当前临时审阅；同聊天资料改变时保留编辑窗口并拒绝过期写入。审阅期间阻止另一个事件任务启动，无关楼层卸载不能释放当前操作锁。
8. 自动/批量事件任务维持现有校验保存流程，不逐条弹审阅。没有新store、持久字段或迁移；逻辑包仍v4，物理IndexedDB仍v1。原始回复及编辑草稿只在当前窗口内存中保留，不存聊天/核心备份，不上传诊断日志。

## 有限验证和实际结果

- 受影响集 `npx vitest run src/mnemosyne/manual-workflows.test.ts src/api/client.test.ts`：44/44通过。覆盖有效/无效/空/截断返回零事件写入、字段错误与500字、修正不重调API、确认原子关联、防重复、宿主改变后拒绝、主/副API空响应、流式末行和HTTP失败。首轮位置断言发现V8并非对所有语法错误都提供行列；界面仅显示真实可得位置，不捏造位置。
- 用户追加重写前，一次全量为23文件365/365通过。追加后受影响 `manual-workflows.test.ts` 为30/30通过，新增归档且无待整理成员时可重写、指令/正文/当前概要进入请求、确认前不改库、保留成员/进展/归档、空链及预算不发送。
- 最终 `npm test`：**23文件367/367通过**；既有脚本810、12、53断言通过。日志 `/tmp/w01-event-rewrite-test-final.log`。第二次全量由用户追加功能触发；未因文档、截图或上传再测全量。
- `npx vue-tsc --noEmit` 通过，日志 `/tmp/w01-event-rewrite-types-final.log`。`npm run build` 通过：CSS105.32kB / gzip16.03kB；JS757.04kB / gzip263.51kB。日志 `/tmp/w01-event-rewrite-build-final.log`；保留既有单包>500kB提示，根目录安装产物已更新。`git diff --check` 通过。
- Chromium153合成浏览器，390×844：`MN_CHROMIUM_PATH=/tmp/w01-event-chromium/chromium MN_TEST_FONT_CSS=/tmp/w01-event-tools/node_modules/@fontsource/noto-sans-sc/400.css node scripts/daily-manual-ui-smoke.mjs` 通过，日志 `/tmp/w01-event-rewrite-ui-final.log`。覆盖正确返回等待确认/取消零写入、空/截断弹窗、原始正文对照、错误字段禁写、修正后一次写入、取消深浅配色、视口布局、无关楼层卸载锁、归档链重写入口位置/改写输入/API取消与确认/审阅保存；继续覆盖既有提示词、菜单裁剪、归档/撤销、编辑/删除及正文补表。无pageerror，无真实API请求。
- 首次重写截图抓到两个窗口切换的动画中间帧，测试改为等旧窗口离场后取图，复查静态布局通过；没有因此修改产品逻辑或重跑全量。合成截图：[字段错误](w-01-ui/event-response-invalid.png)、[修正待确认](w-01-ui/event-response-valid.png)、[改写要求](w-01-ui/event-rewrite-instructions.png)。无用户截图或真实聊天入仓。

## 使用、限制与回退

- 楼层新建/立即更新：确认发送 → 看“审阅事件返回” → 需要时修改 JSON → 格式通过 → 确认写入。字段说明和原始正文在窗口下方展开；修正和确认不会新增一次API请求，取消不保存本次事件/关联。
- 重写：长按折叠事件（含归档页） → 归档按钮下“重写概要” → 输入要求 → 重写 → 确认发送 → 审阅 → 确认写入。更新后仍是同一条链。旧关联、旧版本和归档保留。
- 原失败返回未提供，无法断定那一条正文失败原因。现能看到接口提供的原始输出正文；主API的宿主可能已先行清洗，不展示独立思考、HTTP响应或真实结束原因。SSE末行修复是源码发现的真实边界，但不能认定就是用户此前失败的根因。
- 网络/服务端失败没有成功返回正文时仍走失败提示。疑似截断提示不能代替服务商的输出上限/结束原因诊断。草稿未持久化，刷新、关闭窗口或切聊天后不保留；发生来源变化时先复制所需内容再重新打开。
- 本轮只在Node/fake-indexeddb和合成浏览器验收，未用用户真实RP、付费模型或TT手机。手机最短验收：更新本分支并重载 → 重试曾失败楼层，查看原始返回/修正/确认 → 长按一条现有或归档事件，输入改写要求，确认后检查旧链概要与历史。状态仍 **implemented_unverified**，交Chat review与TT手机实测。
- 可回退源码至开工 `fdd51ad`（或撤销本轮实现提交）；数据库格式无变化，已确认保存的事件仍可读取，不清理、不重写数据。回退会恢复旧的模型返回直接解析保存流程；需要保留旧概要可从历史版本/已有备份核对。

---

# W-01 历史回执：事件概要提示词编辑与归档收纳

日期：2026-09-24。状态：**implemented_unverified**。本节是最新回执，下方保留历史记录。

## 版本、授权与实际范围

- 开工 HEAD `d263ea4305d17636a0e391f7dfb99b3c4b168b19`，分支 `work/daily-memory-mvp-baibai`，工作树干净，fetch 后远端一致；柏宝书固定基线仍为 `393873acd27906a09308ae65fe7e636a3d3941ab`，本地基线 checkout 已核对。已读 AGENTS、当前 W-01 及既有13/14/15/stage规则，单线执行，无子代理。
- 用户先要求在原设置编辑事件概要提示词，随后明确追加长按归档/归档入口，再补充归档卡片折叠时只显示标题。本轮将三项一起交付，没有等待额外确认或创建新任务卡。
- 本地提示词检查点 `e03e8651a373e6063dba39d3a54a3f69023d6ebb`；最终本地实现提交 `446a9a8bbc8b7a41bbdf46cc16afdb1a73b5d586`。远端合并实现提交 `e41ab801b555fbf28ca5ce24d107fedd61a7e593`，与本地最终实现整树均为 `783a6fd5d005b24dbb4a0145874bbebba46ae383`，22个 blob 逐个核对。回执随后单独提交，非强制更新原分支。
- 相对开工源码实现 +229 / −30行；测试/浏览器 +384 / −15行。分两次报告的实现估计合计240～360行，因直接复用编辑器而略低；测试估计合计190～320行，增加了真实编辑器设置持久化/新页面回读、归档召回等价/在途请求、v3/v4恢复及完整移动端交互，超过上限64行。构建、文档、截图不计入源码行数。

## 实现结果

1. 设置 → 自定义提示词新增“事件概要提示词”，复用摘要/二次总结的同一弹窗、默认/已自定义标记、草稿、取消、完成和恢复默认。无宏的模板不显示空宏栏；文本框具有对应可访问名称。
2. `prompts.eventOverview` 随现有宿主扩展设置保存，旧设置缺字段或新字段类型错误时回落空串。默认预填原概括规则；非空自定义替代写作规则，留空恢复内置。新建事件及立即/批量/自动更新每次请求读取当前设置，从下一次请求生效，不自动重写历史概要。
3. 输出 JSON、人工选定边界及500字上限作为独立请求约定保留，程序仍校验响应。自定义提示词不新增费用请求、不混入其他任务设置；它属于全局宿主设置，不包含在档案核心包中。
4. 主页长按折叠事件卡片弹出归档选项；移动超过阈值、松手、取消、移出或卸载时清理计时，长按不触发展开。桌面右键和 Shift+F10 也能打开操作。新建事件右侧增加归档入口，可取消归档回主页。
5. 按用户最后补充：归档列表折叠时**只有标题**；关键词、状态/关联数和操作均隐藏，展开后显示概要、完整追加史及编辑/删除。主页与归档复用同一详情和表单；归档操作不调用模型。
6. `EventChain.archiveSchema=1 / archived:boolean` 只记录当前界面收纳。写入事务核对宿主 guard、分支、Head及视图；不改事件事实、叙事版本或 branch epoch。这样在途概要更新仍可正常提交，且不会覆盖归档标记。事件查询、关联、模型更新和召回不按归档过滤；只有页面分组。
7. 核心包升级 **v4**，成对校验归档 schema/布尔值并完整往返；继续读取 v1/v2/v3。导出文件名随包版本生成。物理 IndexedDB 仍为v1，无新store、无批量重写、无自动删除或不可逆迁移。

## 有限验收与成本记录

- 提示词受影响工作流18/18通过，覆盖默认/空白回落、自定义替代、新建/立即更新/手动批量/自动任务实际发送及下一次请求读取新值。提示词版本完成后一次全量22文件348/348通过；随后用户追加归档，才扩大受影响范围重新验证。
- 归档受影响集：`npx vitest run src/mnemosyne/event-archive.test.ts src/mnemosyne/manual-workflows.test.ts src/mnemosyne/tables-migration.test.ts`，3文件34/34通过。覆盖召回文本前后相同、元信息/成员/进展/epoch不变、归档/取消归档、改名保持、跨分支/迟到拒绝、模型请求期间归档不丢标记、v4往返、旧v3读取和非法标记拒绝。
- 最终 `npm test`：**23文件352/352通过**，既有脚本810+12+53断言通过。日志 `/tmp/w01-event-archive-test-final.log`。没有因文档或日志改动再跑全量。
- `npx vue-tsc --noEmit` 通过；`npm run build` 通过：CSS104.20kB / gzip15.84kB，JS747.36kB / gzip259.94kB。保留既有 >500kB 单包提示。日志 `/tmp/w01-event-archive-types-final.log`、`/tmp/w01-event-archive-build-final.log`；`git diff --check` 通过。
- 合成 Chromium153，390×844：`MN_CHROMIUM_PATH=/tmp/w01-event-chromium/chromium MN_TEST_FONT_CSS=/tmp/w01-event-tools/node_modules/@fontsource/noto-sans-sc/400.css node scripts/daily-manual-ui-smoke.mjs` 通过。验证提示词编辑/取消/保存/恢复默认、模拟宿主保存及新页面hydrate、内容实际进入stub事件请求；长按归档不展开、移动取消、四按钮同排无溢出、刷新后仍归档、折叠仅标题、展开编辑、右键取消归档、无额外API，以及既有楼层浮层/反馈、500字、删除、正文补表流程。日志 `/tmp/w01-event-archive-ui.log`，无 pageerror。
- 原临时浏览器/字体工具已清理，仅在 `/tmp` 恢复测试依赖，未改项目依赖。Chromium包装器解压遇临时环境chown错误，改用解压二进制/共享库并保留当前所有者后运行；第一次截图等宽字体缺中文字形，给测试夹具补中文字体后复查，通过截图确认。非产品字体或宿主行为修复。
- 合成截图：[提示词编辑](w-01-ui/event-prompt-editor.png)、[归档标题列表](w-01-ui/event-archive-collapsed.png)。测试仅本地fixture，所有外网请求拦截，模型stub，无用户真实聊天、付费API或TT手机操作。

## 使用、边界与回退

- 手机最短验收：更新重载 → 设置/自定义提示词/事件概要提示词，修改并完成、重开确认 → 下一次新建或更新事件检查概括风格 → 长按折叠卡片归档 → 点“归档事件”确认仅标题 → 展开看详情 → 取消归档回主页 → 导出v4、在独立空库恢复检查归档标记。
- 实际 TT 触摸/系统长按、真实模型遵守自定义规则及手机性能仍待用户实测；不把合成浏览器结果标为 verified。
- 归档链继续参与召回和待处理概要更新，状态不会被强制改成已结束。归档标记是当前收纳偏好，不是按历史截止回放的剧情事实；归档页全局批量整理仍处理全部待更新链。
- 提示词修改可恢复默认并完成。若回退扩展到开工提交，旧版会忽略归档展示且不能导出新增归档字段或恢复v4包；保留当前库与v4备份，需旧版完整导出/恢复时使用更新前备份到独立库，不自动删字段/数据。下游仍为 Chat review + 用户 TT 手机实测，状态仅 implemented_unverified。

---

# W-01 历史回执：确认式提示与500字事件概要

日期：2026-09-24。状态：**implemented_unverified**。以下保留当时的实现及验证记录。

## 版本与改动

- 开工 HEAD `68a31c9f846e89204057a97b1a5b13cc2f20e125`，分支 `work/daily-memory-mvp-baibai`，工作树干净，fetch 后与远端一致。柏宝书固定基线仍为 `393873acd27906a09308ae65fe7e636a3d3941ab`。延续用户 W-01 授权，单线执行。
- 远端实现提交 `112c6350f43a9882b6010b7797d13b80a29a8e6e`；本地测试实现提交 `f53839696d98b1f52698e0ce0e1ef3377dca4ff5`，文件树同为 `a606ca05bc80c6c18344c45fec3c3a8386295196`。GitHub 连接器上传逐个核对15个 blob 及整树，本回执随后单独提交，一同非强制更新目标分支。
- 实现源码新增118 / 删除30行；测试新增170 / 删除4行（工作流94行，浏览器76行）。实现处于100～180行估计内；测试比80～140行估计多30行，主要为旧长概要仍可增减关联的兼容反例、确认关闭与保留输入的真实组件流程。构建、文档、截图不计入源码行数。
- `FloorEventAction.vue`：成功、失败、材料超限等提示统一为顶层单按钮 alertdialog，点击“确定”关闭；遮罩和 Esc 不关闭。移除卡片下方和操作表单内重复提示。失败保留输入，切换作用域不显示旧操作的迟到提示；已入库但概要失败时明确分别说明。动作改为“立即更新 / 仅入库”，费用确认和无 API 入库行为保留。
- 事件页：移除每条链的“最新进展”显示块；折叠状态只保留标题、关键词、状态/关联数和操作，展开看概要和完整追加史。移动分隔线到展开内容起点，完整摘要仍全宽。
- 新建/更新共用提示词要求 overview 最多500字，建议250～450、内容少则更短；只保留核心变化、关键决定/结果、对关系或重要转变有实质影响的细节及必要因果，放弃琐事、逐站行程、重复对话和装饰性动作。
- 程序在发布前校验概要上限；按去除首尾空白后的 Unicode 码点计数，包含标点、字母和数字。500允许，501拒绝；模型超限不截断、不覆盖旧概要、不推进 summarized、不自动再发付费请求。关联先已保存时保持待更新。人工编辑也显示相同计数并禁止超限保存，数据入口另有校验。

## 有限验证

- 先运行 `npx vitest run src/mnemosyne/manual-workflows.test.ts src/mnemosyne/events.test.ts`：2文件34/34通过。覆盖新建/更新提示词、500字含非BMP字符边界、超长创建无记录、超长更新不写版本/进展且成员仍待处理、人工超限拒绝、旧700字概要仍能加入/移除关联且不会被自动改写。
- 最终候选一次 `npm test`：22文件 **346/346** 通过，既有脚本810+12+53断言通过；日志 `/tmp/w01-feedback-test-final.log`。
- `npx vue-tsc --noEmit` 最终通过；首次检查指出新增合成旧概要夹具对象的 TypeScript excess-property 类型错误，改为已命名结构化记录后通过，未删减测试。日志 `/tmp/w01-feedback-types-final.log`。
- `npm run build` 通过：CSS103.64kB / gzip15.73kB；JS743.08kB / gzip258.48kB。保留原有单包 >500kB 提示。日志 `/tmp/w01-feedback-build-final.log`；`git diff --check` 通过。
- `MN_CHROMIUM_PATH=/tmp/w01-branch-chromium/chromium MN_TEST_FONT_CSS=/tmp/w01-branch-tools/node_modules/@fontsource/noto-sans-sc/400.css node scripts/daily-manual-ui-smoke.mjs` 通过，390×844 合成 Chromium 真实组件流程。验证裁剪容器外菜单、超预算0 API调用、提示仅确认关闭且不占卡片、确认后输入保留、成功弹窗、两个短按钮、无最新进展块、编辑501禁用/500含emoji允许、完整摘要全宽、移除/删除、正文补表及持久进度；无 pageerror。日志 `/tmp/w01-feedback-ui.log`。
- 浏览器请求全部限于本地 fixture，模型为 stub，没有 TT 设备或真实付费 API 操作。截图已人工查看：[超限提示](w-01-ui/event-feedback-error.png)、[成功提示](w-01-ui/event-feedback-success.png)、[精简事件卡](w-01-ui/event-overview-compact.png)，均为合成内容。

## 数据影响、待实测与回退

- 无数据库 schema/包格式迁移，无旧概要自动压缩或截断。旧概要可手动精简；有新成员待处理时，下次正常更新生成500字内概要。纯加入/移除操作允许原样携带旧长概要，避免已有链无法操作。
- 进展历史继续保存，既有召回仍使用原有界进展节选；本轮仅去掉页面显示块，不修改召回。无用户真实聊天、令牌或生产日志进入仓库。
- TT 手机和真实 Flash 输出质量尚未验证。最短验收：更新重载 → 楼层仅入库后确认提示 → 尝试新建或立即更新并确认结果弹窗 → 事件页确认无最新进展 → 编辑概要查看字数 → 新生成概要检查精炼程度与500字限制。超长模型输出会明确失败，需要用户决定重试，不静默增加 API 调用。
- 回退可切回开工提交 `68a31c9f846e89204057a97b1a5b13cc2f20e125` 的扩展代码，无需数据库回滚。下游仍为 Chat review + 用户 TT 手机实测，状态仅 implemented_unverified。

---

# W-01 历史回执：楼层菜单与按钮布局修订

日期：2026-09-24。状态：**implemented_unverified**。以下为当时的实现及验证记录。

## 版本与范围

- 开工 HEAD：`988db13b77df1344e394cae68a1a1127bc2f64cc`；分支 `work/daily-memory-mvp-baibai`，开工工作树干净、远端一致。固定柏宝书基线仍为 `393873acd27906a09308ae65fe7e636a3d3941ab`。
- 远端实现提交：`a87528cd40e61c66d9b02461e01297db8c60cb29`；本地测试实现提交：`b4d45b200a70b960e89dd8a6fbfcaef77f57bb3d`。GitHub 连接器提交元信息不同，逐个 blob 和整树校验一致，文件树为 `5f34a43299d1494f6a829c43ef7cd53862dd3b81`。本回执随后单独提交，与实现一同非强制更新目标分支。
- 用户明确要求修复菜单裁剪、调整按钮明暗层级和摘要成员布局；延续 W-01 授权，单线执行。无架构变更、数据库字段/迁移、真实聊天访问或模型调用。
- 实现源码新增237 / 删除48行；浏览器检查新增132 / 删除8行。原估计实现150～230、测试80～130行，小幅增加来自浮层监听清理、可视区域边界、键盘焦点和合成裁剪容器反例；构建产物、文档和截图不计入实现行数。

## 改动与原因

1. 楼层菜单原来绝对定位在卡片内部，宿主的裁剪与层叠上下文会截断它，增加内部 z-index 无法解决。现在通过 Teleport 挂到原有顶层 modalHost，在卡片外定位；按可视视口留8px边距、空间不足时向上展开，小视口限制高度并允许菜单内滚动。
2. 点击外部、滚动宿主、改变窗口/可视视口、切换作用域或卸载时关闭并清理监听。Esc 关闭后焦点回到事件按钮，方向键可切换两项；测量并显示菜单之后才设置焦点。菜单两项沿用原新建/加入流程，未改变事件关联或 API 语义。
3. 移除所有按钮强制高亮的规则。新建、保存和主要确认操作保留强调色；取消、刷新、批量辅助操作和移除关联恢复较暗的次要样式，并补齐悬停与键盘焦点状态。事件、表格、补表、档案弹窗沿用同一层级，日间/夜间跟随现有主题变量。
4. 原“完整摘要”和“移除关联”共用横向 flex，展开正文也被迫给按钮让出一列。现在按钮只占标题行右侧，字号11px、最小高度30px；完整正文在下一行占满宽度并可换行。摘要预览最多三行，降低长成员卡的视觉密度。

## 有限验收与实际结果

- `npm test`：稳定最终候选一次全量，22文件 **344/344** 通过；既有脚本810+12+53断言通过。日志 `/tmp/w01-polish-test-final.log`。
- `npx vue-tsc --noEmit`：通过。`npm run build`：通过，CSS103.54kB / gzip15.72kB；JS741.44kB / gzip257.75kB。保留原有单包 >500kB 提示。日志 `/tmp/w01-polish-types-final.log`、`/tmp/w01-polish-build-final.log`。
- `MN_CHROMIUM_PATH=/tmp/w01-branch-chromium/chromium MN_TEST_FONT_CSS=/tmp/w01-branch-tools/node_modules/@fontsource/noto-sans-sc/400.css node scripts/daily-manual-ui-smoke.mjs`：通过。真实 FloorPanel 挂在合成 shadow root 的 overflow:hidden + transform 容器内；390×844 下验证菜单边界、裁剪区外命中、屏幕底部向上展开、Esc/方向键/外部点击/滚动/resize、日夜主题、取消与确认颜色不同、移除按钮尺寸、长摘要宽度等。
- 同一脚本保留新建前 API 确认、一次请求建链、重复加入无请求、事件编辑/移除/删除确认、关闭自动表的正文补填和持久进度等流程。模型为本地 stub，所有外网请求拦截，无真实付费 API。日志 `/tmp/w01-polish-ui-final.log`。
- `MN_CHROMIUM_PATH=/tmp/w01-branch-chromium/chromium node scripts/daily-ui-smoke.mjs`：手机尺寸+桌面表格 CRUD、锁定、拖动拉伸、长按、多选、隐藏/显示、搜索通过；44次表操作事务没有读取正文/摘要 stores，无 pageerror。日志 `/tmp/w01-polish-table-ui.log`。
- 首轮新增浏览器检查发现菜单仍处于隐藏测量状态时无法获得焦点，已改为显示后等待 nextTick 再聚焦，最终通过。没有删除反例或为文档改动重跑测试；`git diff --check` 通过。
- 合成截图：[日间菜单](w-01-ui/floor-menu-day.png)、[夜间菜单](w-01-ui/floor-menu-night.png)、[长摘要全宽布局](w-01-ui/event-summary-width.png)、[补表取消按钮](w-01-ui/backfill-secondary-cancel.png)。未将用户截图或真实聊天加入仓库。

## 待实测与回退

- TT 原生手机仍待用户验收：更新扩展并重载，在屏幕中部和底部各打开一次楼层事件菜单；检查两项完整可点、滚动时关闭；展开长摘要，检查移除按钮只占标题行；检查新建/编辑/补表弹窗取消按钮较暗。
- 合成浏览器验证不代表 TT WebView 的缩放、软键盘、系统触摸与主题现场都已验证；没有声称手机毫秒级或实测完成。事件召回、归链、表格填充、归档和数据库语义未修改。
- 回退可切回 `988db13b77df1344e394cae68a1a1127bc2f64cc` 的扩展代码，无需数据库回滚。下游仍为 Chat review + TT 手机实测，状态仅为 implemented_unverified。

---

# W-01 历史回执：手动事件链与正文补表

日期：2026-09-24。状态：**implemented_unverified**。以下保留当时实现及验收记录；按钮全部强调色的设计已由上方最新修订替代。

## 版本与范围

- 开工 HEAD：`a1536c350c974bdeaffa8e19c05b9e30fdbcbf7c`；目标分支 `work/daily-memory-mvp-baibai`，开工工作树干净、远端一致。固定柏宝书基线仍为 `393873acd27906a09308ae65fe7e636a3d3941ab`。
- 远端实现提交：`d2cc1a5490ccd496ac005493d06802c612fcfbe4`。本地测试实现提交：`14a256185323da43d409bb5411a0459ee1cd6c3c`；两者文件树同为 `59f8510b9f43a74e886bfcb9c77832430e59c4a2`。GitHub 连接器使用不同提交元信息，已验证文件树一致。本回执随后单独提交。
- 用户本轮明确要求覆盖原自动归链设计，以及概要可编辑、确认后完整删除链；延续 W-01 授权，没有自行新建正式任务卡。
- 实际实现新增1405 / 删除256行；测试与合成浏览器脚本新增636 / 删除2行（含432行工作流测试、200行新浏览器脚本及既有脚本适配）。实现落在1200～1800行估计内；测试多于原300～500行估计，原因是增加跨真实组件的楼层卡片到主页面流程、恢复与来源反例。构建产物、文档和合成截图不计入源码行数。

## 实现结果

1. `FloorPanel.vue` 的真实 AI 楼卡片增加事件图标，旁边两行菜单为新建 / 加入已有链。新建说明弹窗使用同套主题，点击新建后再确认一次 API。原生单楼摘要材料构建提取为共享函数，角色卡、用户设定、世界书、此前状态/历史、自定义表参考等按原生规则获取，附本楼正文/摘要和用户说明；一次摘要 API 只生成一条事件链，不重写摘要或其他状态。
2. 手动关联程序强制使用当前有效摘要 ID；已关联本楼跳过，不重复写入。仅入库无模型调用。更新概要发送选定链全部合法成员与正文材料，涵盖所有待更新内容；当前概要/关键词写新版本，本批新进展追加保留。标题/状态在已有链更新时不被模型改动。无待更新成员不再付费请求。
3. 自动和手动批量任务不再调用原自动分类/建链路径。按消息楼间隔更新待处理链，每链一个请求，固定本次目标；同一个批量按钮切换为停止。停止不应用在途结果，成功链保留。原 delay / batchSize 历史配置字段仍兼容读取，但不参与新调度，也不再显示。
4. 事件页按标题、关键词、状态与关联数 / 编辑删除、最新进展布局。展开显示完整当前概要，分隔线后“完整追加史”只显示分页成员摘要；移除关联与完整摘要同行，不再提供原摘要版本下拉输入。新编辑弹窗可改标题/状态/概要/关键词；删除二次确认后在一个作用域事务中删除链、版本、关联、进展和相关旧处理回执，正文摘要及其他链保留。
5. 召回包装函数和预算算法未改：仍用原追加进展的有界节选，保留去重与截止校验。可编辑当前概要暂不直接代替这块注入；事件视图新增概要依赖过滤，移除/失效成员后不继续显示完整旧概要为有效事实。
6. 表列表新增批量补表：多选表、各表最后确认楼号 → 最新楼号 / 总楼数、手填起止范围（0起始）。正文而非摘要，附字段定义/说明/提示词和已有可见行；每表每20楼一个批次。手动请求允许自动填表关闭的表；不改自动开关。提示词要求重叠范围只补缺，不重复新增/更新已有内容。程序继续校验类型、锁定、人工字段、合法行 ID 和版本。
7. 表行与回执同事务提交实际 SourceRef 和楼层范围，无变化也留回执；失败/停止未提交范围不推进。最后确认楼号不是连续覆盖承诺，UI 明说可能存在前段缺口。正文修改后旧回执不算当前已处理；旧来源行不进入有效模型上下文但保留人工查看。隐藏/显示只改可见性，保留来源；主动编辑内容可形成用户确认后的当前行。
8. 档案页删除续聊按钮及“打开摘要页 / 事件 / 自定义表”重复入口，分叉按钮改“继承档案”，仍明确选择来源和条数、核对身份/正文。操作按钮统一采用新建表的强调配色，保留列表行、导航与下拉选择器的中性样式。

## 持久化与兼容

- 无物理 IndexedDB 升级、无不可逆库迁移、无自动清理。事件概要记录 eventSchema=2；正文补表回执 backfillSchema=1，表行可带 bodySources。
- 导出逻辑包 **v3**；仍读 v1/v2，严格校验新增概要引用和正文范围回执。导出包含概要版本、关联、进展、补表行和回执；原排除项（宿主聊天、知识库、密钥等）不变。
- 旧零散事件链保持原样，不自动合并；用户可以把同楼加入目标链，再确认删除不需要的旧链。
- 删除链是用户按钮二次确认后的完整删除，不能撤销；需要留存先导出。历史模型处理函数保留供兼容和既有回归，不再挂到自动调度/UI。

## 实际验证

- 受影响集：事件、桥接、摘要填表、迁移4文件61测试通过。新增工作流最终14/14通过，包含整日约会三楼一链、重复加入、无 API 入库、批量只更新已有链、停止/迟到/坏响应、单次原生材料创建、编辑/删除、关闭自动后的正文补填、无变化进度、重叠范围、正文失效、隐藏来源保留、部分表失败及 v3 恢复。
- 稳定实现后一次 `npm test`：**22文件344/344通过**，既有脚本810+12+53断言通过。日志 `/tmp/w01-manual-test-final.log`。未重复无理由全量；之后仅调整事件标题的 CSS display 使展开箭头与标题同一行，重新 build 与该页面浏览器检查，未重跑单元全量。
- `npx vue-tsc --noEmit` 通过；`npm run build` 最终通过：JS738.74kB / gzip256.69kB，CSS102.55kB / gzip15.55kB；保留原有 >500kB 大包提示。
- 合成 Chromium：`scripts/daily-manual-ui-smoke.mjs` 390×844 通过，真实楼层卡片 + 主页面组件，新建费用确认前0调用、确认后1调用、重复仅入库无额外调用、编辑/取消删除/确认删除、自动关闭表的正文补填与持久进度；主 API 为本地 generateRaw stub，拦截所有外网，无真实模型调用。
- 既有 `scripts/daily-ui-smoke.mjs` 通过手机+桌面表格 CRUD / 长按 / 锁定 / 隐藏与显示 / 拉伸 / 搜索，44次表操作事务未读正文/摘要。`scripts/daily-branch-ui-smoke.mjs` 通过继承档案下拉、空选择禁用、条数和新分支发布。日志分别 `/tmp/w01-manual-ui-final.log`、`/tmp/w01-manual-existing-ui.log`、`/tmp/w01-manual-branch-ui.log`。
- 浏览器调试曾遇到合成测试宿主主界面遮住聊天卡，以及脚本导入第二份 Vue 导致响应式不刷新；修正 fixture 为主界面关闭后使用同一 createApp 挂载真实卡片。截图等待弹窗过渡结束，不把中途淡入当作成品。新增单测一处预期“早餐”与 fixture“早饭”不一致已修正，无删减反例。
- 可视检查：[事件页](w-01-ui/manual-events-mobile.png)、[正文补表](w-01-ui/manual-backfill-mobile.png)。全部为合成数据；未读取用户真实聊天，未操作 TT 桌面或手机，未调用付费模型。

## 尚待实测与回退

- 用户手机的点击速度、长链模型上下文容量、Flash 的概要质量、对重复事实的语义去重仍未验证。程序保证成员边界和操作合法性，不保证模型能完全识别所有已有同义事实。
- 事件完整链材料超预算会暂停，不截断；可调整字符预算和模型上下文。补表在当前表状态上补缺，不是历史状态回放；每20楼一批，部分失败保留之前已提交批次。
- 建链说明用于初次生成标题/概要，后续用这些结果和全部成员继续，不另存一份独立链规则。仅入库需本楼有有效摘要；源消息身份缺失仍不能强行继承。
- 回退前保存 v3 包及宿主聊天。旧扩展不理解新字段，不能用旧版覆盖/导出新格式；若需完整回退功能，使用更新前备份恢复到另一个库，并保留当前库与 v3 包，不自动删除。
- 手机最短验收：更新重载 → 有效楼层卡片建一条整体约会链 → 后续两楼仅入库，确认没有 API → 收束楼更新一次概要 → 事件页编辑 / 取消删除，再对合成链确认删除 → 自动填表关闭的表从正文范围补填，检查最后楼号 → 导出恢复检查概要和补表进度。召回另由用户后续实测，不标 verified。

---

# W-01 最终回执：分支名称选择修订

日期：2026-09-24。状态：**implemented_unverified**。本节覆盖下方历史回执的版本说明。

- 开工 HEAD：`2a08ab075e81ca9ee801a2fab82ac0541cbf205c`，分支 `work/daily-memory-mvp-baibai`，工作树干净、远端一致；固定柏宝书基线 `393873acd27906a09308ae65fe7e636a3d3941ab` 不变。用户明确要求改用下拉菜单，延续 W-01 授权。
- 实现：档案冲突页按当前角色 / 群组的归档聊天名选择来源，优先选中继承绑定；空选择禁用操作。列表只读 host_bindings / branches / history_snapshots，迟到结果核对宿主作用域、代次和活动库。名称取已有绑定记录，不把名称当永久身份。
- UI：将“父分支前缀”解释为开头消息条数，预填两边较短长度并标明仅为建议；保留现有身份和正文核对，原分支不变。续聊、独立新故事和分叉的作用分开说明。分叉不复制事件链、自定义表。
- 实际源码：实现新增87 / 删除9行；单元测试新增42 / 删除1行，另新增独立合成浏览器检查脚本（约100行）。比最初测试预估增加一个真实下拉选择与提交的浏览器验收脚本。安装产物重新构建并随源码提交。
- 有限验证：受影响 `npx vitest run src/mnemosyne/bridge.test.ts` 20/20；最终 `npm test` 21文件330/330，脚本断言810+12+53；`npx vue-tsc --noEmit` 通过；`npm run build` 通过，JS728.13kB / gzip253.19kB，保留已有大包提示。
- 浏览器：`MN_CHROMIUM_PATH=/tmp/w01-branch-chromium/chromium node scripts/daily-branch-ui-smoke.mjs` 通过。合成 Chromium 390×844 验证来源预选、展开选择、空选择禁用、建议条数和明确分叉产生第二分支；无页面错误，无外网或模型调用。首轮脚本因自动滚动触发共用下拉菜单关闭而超时，脚本先将选择器滚到视口中间后通过；没有改共用组件，也没有声称原生 TT 触摸行为已验证。
- 无 schema 改动、无数据迁移、无用户真实聊天或付费模型测试。原生 TT 手机尚待用户实测；来源名称是已保存归档绑定名，未归档聊天不在列表。父分支后续修改导致旧前缀不匹配时仍拒绝，不扩展为历史快照选择器。
- 手机最短验收：更新扩展并重新加载 → 在原会话确认已归档 → TT 创建支线 → 档案页确认预选原会话名称及继承条数 → 点“从所选聊天分叉” → 等已归档，再回原会话确认内容仍在。另开支线不要点“沿用所选分支继续”。
- 回退：切回开工 commit 的扩展代码即可；无需回滚数据库格式。已经显式创建的分支仍保留，不自动删除。下游仍为 Chat review + TT 手机验收；最终提交见包含本节的 Git commit，远端 SHA 另在交付消息给出。

---

# W-01 最终回执：手机反馈修订

日期：2026-09-23。状态：**implemented_unverified**。最新用户说明：[档案 / 迁移、事件与自定义表](../docs/daily-memory-user-guide.md)。

## 本轮版本与授权

- 用户明确要求补充使用说明、排查页面卡顿，并按描述重做自定义表；沿用 W-01 授权与目标分支，未新建正式任务卡。
- 开工分支：`work/daily-memory-mvp-baibai`；HEAD `8e91e7fccccfcde8fd04ca43aaa7f0d845518d89`，工作树干净。
- 柏宝书实现基线仍为 `Roballz/ST-BaiBai-Book@393873acd27906a09308ae65fe7e636a3d3941ab`。
- 远端本轮主实现：`ad97601f8ac87fa613e2745faf67955346853612`；最后实现修复：`28e8756cfe23981e064b5ae049111e482cdb875c`。本回执随后单独提交。
- 本地最终测试提交：`31d946101d115a2466a7e4a4a4d06b8808123dde`；与远端最后实现的 Git tree 均为 `6aee0a190b65ff4b47f2f512dd6966b36f0fd164`。GitHub 连接器的提交元信息不同，文件树一致。
- 写入通过已有 GitHub 连接器权限完成，非强制更新本分支；不修改 main，不部署服务。

## 改动与性能原因

1. 旧档案 / 表格页每次 mounted 都调用 `syncDaily()`：全聊天观察、保存元信息、canonical 同步、逐条加载正文/摘要。档案页还重复计算有效性；表格页将全量摘要渲染成勾选框。这是源码证据，与用户等待状态/按钮逐步亮起的描述吻合。
2. 原生摘要页主要使用已在内存的 `chat.extra.bbs_leaf` 和 `memory.summaries`。此次保留原生摘要页，移除档案默认正文/摘要列表和表格摘要选择列表；待审核项只在点击时加载。
3. 档案页直接显示后台归档状态；表列表只读取当前绑定和表定义，选表后才读该表行。页面不再以重新归档为打开前置，手工改表也不重新读取正文或摘要。
4. `syncDaily` 对未变宿主/Head/视图复用已完成缓存；仅事件/表格/审核 epoch 改变时复用正文视图再更新有效性。作用域、数据库、Head、选择、宿主版本和观察代次仍核对。
5. canonical 独立读取排队并行；来源/记忆版本未变化时跳过逐对象历史扫描；摘要有效性按共享基线/历史块读取一次，并用 Map 查询身份与依赖。有真实 inputRefs 的摘要不重新展开整段历史基线。
6. 原先注入循环每条摘要都执行 `hostVersion()`，反复序列化全聊天；改成每次构建注入视图只捕获一次允许集合。快路径仍在异步读取后复核宿主版本，不以去掉保护换速度。

没有宣称手机毫秒级，也没有将合成计数冒充用户设备耗时。首次归档、超大表单表 getAll、大库导出和手机后台存储仍可能等待；本轮不做生产手机容量压测。

## 自定义表与注入

- 表列表为入口，右键 / 长按 / `···` 管理表。设计页列出字段三行信息；单字段弹窗含名称、中文写入策略、类型、介绍、AI 填写提示词。行列表只预览前两字段，详情弹窗逐字段编辑，可拖动输入框右下角扩大。
- 支持长按进入多选、全选本页、隐藏 / 显示行、搜索与20行分页、改名和确认删除；删表/字段/行保留归档数据。
- 覆写、追加、首次写入后锁定；0/false 算已写入。AI 与手工编辑都拒绝改锁定值；用户可先修改字段策略。旧 manual 字段仍仅手动，不转成 lock。
- **同次原生摘要 LLM 请求**附加表定义、字段说明/提示词、当前可见行，并在原 JSON 根对象增加 `customTables` 的 add/update。单楼、用户自定义摘要提示词、包含最新楼的批量摘要均接线；无独立填表 API 请求。
- 纯历史补摘 / 高层压缩不读取当前表冒充历史状态。包含最新楼的批量摘要返回整批净变更；尚未具备自定义表完整历史状态重放。
- 结果先按 schema 和合法 ID 校验；表/行版本改变或宿主变化拒绝迟到结果。摘要与操作 ID 随聊天先保存，新库中各表行和回执同事务提交。明确无变化也留回执；失败显示对应楼号和可重试状态。
- 重摘同一楼会替代该楼旧未提交请求，即使摘要文本未变；已经提交的表格行/回执不回滚，不重复追加。核心包仍不包含未成功保存到库的宿主 sidecar，存在待处理时需先解决或另备份宿主聊天。
- 有效可见表行并入原 `mnemosyne_memory_state`：D1 / D2 跟随原状态位置；仅摘要模式关闭状态注入。隐藏行既不发给摘要 AI，也不发给正文模型；关闭自动填写不等于隐藏。
- 事件仍独立整理，召回补充在原候选最终选中后，与摘要/原文和知识库合并进原召回槽，使用原混合召回注入深度。页面和使用说明逐项解释间隔、延迟、批量、链数与额外进展。

## 数据兼容与代码量

物理 IndexedDB 维持版本1和既有固定 stores，不自动删库，不覆盖旧柏宝书。自定义表定义协议升级 `tableSchema=2`，新增字段提示词、lock、行 hidden 与 dataVersion。核心包导出格式 v2，恢复向后兼容 v1；旧版扩展不能读取新 v2 包，回退须保留新版包并使用旧备份。

相对本轮开工 `8e91e7f`，不计文档、截图和 dist：

| 类别 | 新增 / 删除 | 口径 |
| --- | --- | --- |
| 实现 | +2,781 / −536 | src，排除测试；净增2,245 |
| 测试 / 浏览器 harness | +555 / −30 | *.test.ts 与 scripts/daily-*；净增525 |

开工估计实现1,300～2,100、测试250～450行。原始新增数超过实现估计上限约32%，包括完整的字段 / 行弹窗、移动端拖动 / 长按及为可读性展开原有紧凑 TS/Vue/CSS；净实现增量约比上限高7%。未新增项目运行依赖、服务或数据库栈。浏览器 / 格式化 / 测试字体工具只位于临时测试环境。

## 本轮有限验收与实际结果

| 验收 | 实际证据 |
| --- | --- |
| 页面无全量正文 / 摘要渲染 | 合成浏览器档案页无原列表；表格操作44次事务，source_revisions / memory_revisions 读取为0 |
| 缓存刷新不重复保存宿主 | bridge 测试：未变视图重复刷新仅分支元信息只读，不 saveChat / saveMetadata / 全扫描 |
| 共享基线读取有界 | 80摘要合成夹具：一次读取共享 snapshot、两块 manifest、一次 reviews 查询 |
| 同一次摘要填写 | 单楼默认 / 自定义提示词及最新楼批量集成测试，模型调用各1次，表结果保存，原 delta 不混入自定义表 JSON |
| 写入策略与权限 | lock首次写入含0/false、人工/AI后续修改拒绝；append/replace；隐藏/未知表行/字段、错误类型、缺字段拒绝 |
| 恢复与迟到保护 | 宿主保存失败重试、表格期间修改拒绝、跨表事务中途失败整体回滚、重复提交不重复追加、旧请求被重摘替代 |
| 包格式 | v2表字段与隐藏行往返一致；旧v1包与manual策略恢复保持ID；物理库版本仍1 |
| 手机尺寸交互 | 合成Chromium 390×844：表/字段/行操作、锁定、拖动放大、长按多选、隐藏/显示、搜索，无pageerror；另检查1100×860布局 |

命令与最终结果：

- `npm run build`：通过，最终 dist JS 725.13 kB / gzip 252.01 kB，CSS 100.70 kB / gzip 15.19 kB。仍有原有单包 >500kB 提示。
- `./node_modules/.bin/vue-tsc --noEmit`：通过。
- `npm test`：**21 个文件 / 327 项通过**；timeRel 810、vector-depth 12、memory-regressions 53 项断言通过。
- `MN_CHROMIUM_PATH=... MN_TEST_FONT_CSS=... node scripts/daily-ui-smoke.mjs`：上述合成浏览器检查通过。截图在 `notes/w-01-ui/`，不是 TT 手机截图。
- `git diff --check`：通过。

测试成本记录：先跑受影响集成；第一轮全量326通过后，发现“重摘旧楼仍残留待提交表请求”的真实恢复问题，修复单楼/批量替代路径并加反例，最终重新构建、类型检查和全量327通过。没有因文档改动重跑测试。浏览器断言修正了重复文本定位和弹窗退出动画等待；最终UI交互通过。之后仅改逻辑包版本/有效性读取及旧请求替代路径，无UI布局变化，未重复浏览器矩阵。

## 用户验收、限制与回退

按使用说明的五步短流程更新分支、查看页面、建表/隐藏、生成一条新摘要、导出恢复。实际手机耗时、长按系统行为、后台中断、真实模型遵守填表协议及TT注入现场仍待用户验收；未调用用户付费模型，未读取用户聊天或操作设备。

停用新扩展并启用原柏宝书可回到原体验，保留宿主聊天备份、旧柏宝书库和新核心包。若退回W-01较早版本，使用更新前的备份，不能让旧代码读取新表策略。无自动purge、不可逆物理迁移或VPS变更。

---

## 首次交付历史回执

以下保留首次交付当时的记录；其中“手动选择摘要填表”“浏览器未能运行”等已由上文本轮结果取代，旧测试数不作本轮重跑证据。

# W-01 最终施工回执

状态：**implemented_unverified**。仅交 Chat review 与用户 TT 手机实测；未标 verified。

日期：2026-09-23（UTC）。目标：`Roballz/liminal-axis-mnemosyne` / `work/daily-memory-mvp-baibai`。

## 版本与工作树

- 开工目标 HEAD：`cd728fde7b133b41db512efbc5c1358f439c03d4`，工作树干净。
- 柏宝书固定基线：`Roballz/ST-BaiBai-Book@393873acd27906a09308ae65fe7e636a3d3941ab`，实际 detached checkout 一致。
- 远端实现及最终构建产物 commit：`f1d735d74f6bbb60a1132e0fbe68e906f56d1e57`。
- 本地实际测试实现 commit：`64ef43370d52a7e5218933a1e5c6b0ca5ade93f1`。远端通过GitHub连接器创建的commit元信息不同，但tree精确相同：`84630284c38d30600b3b06772c277b3517c36b69`。
- 本回执为后续纯文档提交；上述实现 commit 已包含最终测试的源代码与 dist。
- 推送前 fetch 核对远端仍是开工 HEAD。CLI HTTPS push 因无写入凭据失败；GitHub连接器确认push权限后上传Git对象，逐个核对大文件blob SHA，并核对整树SHA一致，最后使用非强制ref更新。未强推、未改主分支。最终发布HEAD由本回执所在提交及Git分支记录定位。
- 单线施工，无子代理。已读 AGENTS、W-01、13/14/15/stage 及06/08/12契约。未生成后续任务卡。

## 实际改动与行数

| 范围 | 实际差分 |
| --- | --- |
| 相对固定柏宝书基线的实现增量（src及实现脚本） | +2,198 / −71 行 |
| 新增测试及合成 fixture/browser harness | +928 / −0 行 |
| 实现提交相对原 Mnemosyne 分支的整体差分 | 133文件，+38,605 / −2 行；含原样上游、文档、lockfile及生成产物 |

统计口径：逐文件与固定上游比较；`.test.ts`、测试脚本、fixture、smoke harness归测试，其余TS/Vue/CSS/实现脚本归实现；生成产物、依赖锁、manifest、文档不混入实现增量。

开工预估实现3500～6000、测试900～1800。实现低于下限约37%，施工中已报告新估算约2200行：直接复用原生成器、检索/配额、组件及现有渠道，采用薄的原生IDB模块，没有重造引擎或UI。测试接近原下限。代码量偏差不是范围授权变更。

主要模块：

- `src/mnemosyne/model.ts / db.ts / canonical.ts`：独立schema、IDB事务、正文版本、分块历史清单、映射、摘要选择/来源审核、固定前缀分叉。
- `bridge.ts`：宿主自有消息标记、增量幂等同步、跨存储pending/retry、观察代次、正文/摘要来源接线。
- `events.ts / jobs.ts`：全量合法目录、独立批量/手动补齐、成功回执、none/needs_review、只追加进展、人工锁定、截止一致的有界包装。
- `tables.ts`：固定defs/rows、类型与更新模式验证、手动增量AI填写、无变化成功回执。
- `json.ts / migration.ts`：重复JSON键拒绝、格式/计数/引用/领域检查、固定事务导出、独立空库恢复及成功后切active。
- `pages/archive / events / tables`：档案/审核/来源、事件折叠与成员分页、进度与设置、自定义表、迁移入口。
- 原 `memory/engine、apply、inject、vector/index、recall、hybrid、knowledge`：最小接线与独立命名；根 manifest/dist 可安装。

## 构建与测试事实

所有模型路径只使用 stub/固定响应；未读取用户真实聊天、未调用用户付费API、未连接用户手机。

| 命令/检查 | 本轮结果 |
| --- | --- |
| 固定未修改上游 `npm run build` | 通过 |
| 固定未修改上游 `npm test` | 14文件/244测试；脚本810/12/53断言通过 |
| 受影响组测试 | 按新增存储、事件、表/迁移、bridge、adapter和真实失败逐组执行；全部收敛通过，不累加重复执行数冒充独立测试 |
| 最终实现 `npm run build` | 通过；JS 697.85 kB，gzip 242.50 kB；CSS 94.71 kB |
| 最终实现 `npm test` | **20文件 / 308测试通过**；timeRel **810**、vector depth **12**、memory regressions **53**断言通过 |
| `./node_modules/.bin/vue-tsc --noEmit` | 通过 |
| 根部安装入口静态检查 | manifest的JS存在、CSS存在、`mnemosyne_generateInterceptor`存在 |
| `git diff --cached --check` | 通过 |
| `node scripts/daily-ui-smoke.mjs` | 未完成：环境没有 Chromium 可执行文件；下载返回无效ZIP，停止重试。没有渲染/点击通过证据 |
| TT桌面 / TT手机版 | **未运行**，由用户验收 |

第一次候选全量为307测试通过；之后源码复核发现高层审核接线仍沿用上游移除行为，并补了事件发送前版本复核。实际修改这两处后，先跑受影响集，最后再次构建/全量得到上面的308测试。第二次全量由真实实现变化触发，不是重复“再确认”。夹具缺`queryIndex`的类型错误也已修正。

环境说明：pnpm 11 安装命令完成依赖下载但以esbuild安装脚本未批准为由返回非零，后续pnpm依赖状态检查同样阻断；现成平台二进制可用，使用npm执行源项目脚本成功。未改供应链/权限策略。Vite有原有大chunk警告，没有为本任务另做性能重构。

## W-01 二十项反例映射

| # | 反例与对应证据 | 层级 |
| --- | --- | --- |
| 1 | canonical：同文本不同消息/故事不合并；固定分叉复用有证据的身份 | Node/fake-IDB |
| 2 | canonical/bridge：edit创建Revision，摘要待审；保留仅当前Head，后续再编辑重审；上级按依赖显式重建 | Node/fake-IDB |
| 3 | canonical：A→B→A复用历史Revision，新Head不复活旧run | Node/fake-IDB |
| 4 | canonical：删除退出当前视图，旧正文/摘要仍保留 | Node/fake-IDB |
| 5 | canonical/bridge：重复同步不新增对象 | Node/fake-IDB |
| 6 | bridge：宿主成功/IDB失败保留pending；宿主保存失败重试身份标记 | Node/fake-IDB |
| 7 | events：明确none写success且不重复处理 | Node/fake-IDB |
| 8 | events：解析缺字段/保存失败均不推进；注入磁盘失败回滚全部事件写入 | Node/fake-IDB |
| 9 | events：5条摘要→5关联+1进展 | Node/fake-IDB |
| 10 | events：同链多个正常命中只包装一次，不删除命中 | Node/fake |
| 11 | events：latest在命中/近期集合时不补充 | Node/fake |
| 12 | events：旧snapshot/cutoff不读未来标题、成员、计数、概述；跨批次段整段排除 | Node/fake-IDB |
| 13 | events：25成员链仍固定字符预算、至多1额外进展 | Node/fake |
| 14 | tables：增表/字段不升级物理IDB版本 | Node/fake-IDB |
| 15 | tables：AI写manual字段/删整行/未知字段/类型错误拒绝 | Node/fake-IDB |
| 16 | tables-migration：事件、正文、摘要、custom行全包往返逐对象相等 | Node/fake-IDB |
| 17 | tables-migration/boundaries：坏计数、checksum、引用、selection、事件结构拒绝，原active不变 | Node/fake-IDB |
| 18 | adapter/既有knowledgeRecall：canonical不可用仍独立知识库；旧库只读复制并可搜索 | Node/fake-IDB |
| 19 | 独立manifest/interceptor/注入命名源码检查；README/档案页明确停用原柏宝书 | 静态；双扩展及实际注入待TT |
| 20 | bridge/events/adapter：观察代次、正文、事件epoch变化拒绝迟到结果，发请求前/发布前复核 | Node/fake-IDB |

另覆盖全量目录超预算暂停、已有链续接、reference/none/needs_review、多事件、操作幂等/冲突、人工移除锁不能被AI重新激活、自定义表已有行/选定范围/无变化回执、严格JSON重复键等。

## 数据、兼容与重要实现选择

1. canonical事实与向量投影独立。IDB事务中只作数据库请求；hash、模型与网络在事务外。历史清单按128条引用分块共享，追加不复制整段正文。
2. saveChat与IDB不伪装成原子事务。宿主消息身份先保存；宿主metadata保留pending；失败可重复观察重试，不换ID盲写。前端另显示错误。
3. L0实际正文引用记录在生成前；非标准回合完整归档但不伪造User+Assistant coverage。旧资料无证明时保留声明。高层保留实际child版本依赖；旧生成上下文不补造。
4. 上游自己写入的物品/变量旁注会产生正文新版本。仅完全匹配本次程序写出的内容、且其它实际输入未改变时，写`origin=generated_sidecar`兼容事实；生成inputRefs不改。用户编辑走人工review。
5. 事件概述只追加。元信息/关联/进展/回执同事务；人工关联使用保留历史的锁定修订。失效或被移除关联依赖的段不注入。
6. 全量目录与本批材料采用明确字符预算；先确定批次截止再读取目录，不带后面楼层的事件信息。事件额度默认2链、每链最多1额外摘要、1600总字符，设置可改。
7. 默认不开自动事件调用。手动补齐固定目标、逐批推进；停止不冒称取消宿主请求。needs_review持久化并等待人工处理，不自动反复付费。
8. 自定义表用固定stores。删表/字段/行确认后逻辑隐藏，旧字段单元格保留但不再发给AI；类型改变要求新字段，不偷偷转换历史值。自动周期填表未做。
9. 根部保留可安装产物。物品/地点/生活档案、时间/地点与原UI继续读取旧聊天delta。独立公共接口/按钮/设置/注入命名避免静默覆盖原扩展。
10. 迁移核心包明确排除宿主聊天/delta、知识库、索引、密钥。旧知识库另有显式只读复制本地文件与向量入口，按完整embedding身份判断可用；不称核心包为完整柏宝书备份。

## 偏差、未验证与限制

- 实现增量小于预估，原因和口径见上；不把原样导入3万余行当成本轮新设计。
- 旧后端bundle没有canonical身份/来源证明时不直接进入新召回；这会减少该特定旧档路线候选。需要先归档旧聊天并明确续接/分叉，或保留种子高层的兼容展示。未声称旧后端bundle无损全量转换已经完成。
- rerank失败时，真实rerank分数为null；为了保持基线选档行为，仍独立使用余弦阈值选择回退原文。此差异有adapter反例，不将余弦值作为rerank展示。
- 当前实现对所有canonical对象作全包逻辑导出，导入亦需在内存校验。大库/超长目录性能、手机存储回收、后台恢复与真实host保存事件顺序未做容量/设备验证。
- 未执行真实模型质量评估、TT安装、双插件现场、桌面/手机UI测试。合成浏览器harness保留可复现入口，但当前环境未能运行，不把截图/页面断言写为通过。
- 保留旧B provider和主线历史资料，不做物理迁移，不重跑T04/T05原生/强杀/性能矩阵，不访问私人档案。

## 用户最短验收与回退

1. 备份宿主聊天，停用原柏宝书，按README安装本分支。
2. 打开合成/明确愿意测试的聊天：状态功能、时间/地点、知识库与原混合召回均检查一次；档案显示已归档。
3. 生成数条摘要，做一次编辑→保留/重新生成，检查旧版本仍在、待审核状态正确。
4. 手动整理事件，展开看成员；同链中间命中检查有界注入，确认最新已在近期全文时不重复。
5. 建自定义表，人工编辑与一次选定摘要AI填写，manual字段不被修改。
6. 核心包导出→新空库恢复→重启，确认ID/正文/摘要/事件/行仍一致、不串聊天。
7. 记录TT版本、手机环境、实际问题与注入片段（分享前脱敏），交Chat review后再判断验证状态。

回退：停用本fork，重新启用原柏宝书；保留旧库和聊天备份，以及Mnemosyne核心导出包。不要自动删库，不把核心包当作宿主聊天/知识库全备份。无任何自动purge或VPS变更。

下游仅依赖：Chat源码review、TT桌面/手机人工验收及由其反馈驱动的修复。不创建下一张正式任务卡。
