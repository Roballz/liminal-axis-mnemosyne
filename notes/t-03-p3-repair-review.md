# T-03 P3 返修验收与 P4/P5 放行

日期：2026-09-21。审核实现：`4a76c142bc58466fea2728020f5856c3f63e8cb3`；返修前实现：`75abeeab496b7b129083f795e3935a54367e47e5`。

## 1. 结论与当前许可

**P3-R1、P3-R2 关闭；本轮受控 P3 集成的高难项 review 通过，允许继续原 T-03 的 P4 → P5。** 本次返修范围未发现阻止进入该验证/收尾阶段的新问题。T-03 总状态保持 `in_progress`，S-A 未完成，不能提前升级整个任务为 verified。

本回执覆盖旧 AGENTS、task、handoff、阶段导读及 `t-03-p3-integration-review.md` 中“等待 P3 返修审核、不得进入 P4/P5”的临时状态；不删除历史实施记录，不新增任务卡。原 T-03 的产品范围、验收矩阵及安全权限保持。P4/P5 的开工与结果写回原 task，完成后交最终 review，不自动创建/实施 T-04。

**放行不是手机生产准入，也没有关闭自动外部维护缺口。** 继续保留 `HOST_MAINTENANCE_UNSUPPORTED`，只在既定独立实例、合成数据、无未经协调的同步/归档/换库条件下执行。不改 TT、不自行切 B2/A、不用开关或人工承诺假装获得原生 fencing。

## 2. 独立执行与证据边界

仓库执行摘要：`evals/t03/p3-review-repair/chat-review-4a76c14.json`。

Chat 在隔离 Linux / Node v22.16.0 中按固定提交准备源码，22 个文件的 Git blob 哈希一致，22 个文件 `node --check` 通过。源码是连接读取后原样落到临时目录并校验的，不把下载失败后的手写近似代码当作被审版本。

实际命令：

```sh
node --test --test-reporter=tap packages/storage/tests/p3-review.test.mjs
```

结果：**5/5 通过，0 失败、0 取消、0 跳过。** 使用原仓库完整定向测试文件；包含实际 PagedDomain、分页业务编译、coordinator、重开和完整导出/恢复，IO 为 FakeIO，不是真实磁盘、TT 或手机测试。

另外已读 Codex `verification.json`、`after.tap` 的 319/319 汇总及提交中的两个旧实现失败记录。**本轮没有独立重跑完整319项，也没有独立重跑旧实现对照或 TT 原生实验。** Codex本次返修也明确 `nativeRerun:false`；既有分页强杀证据继续保留原提交范围，不改名冒充修复后真机验证。

## 3. P3-R1：精确成员校验——通过

`PagedDomain.indexMember()` 同时核对 members 引用和当前 order 中该 label 的精确引用；member、reorder 和插入去重使用同一判定，不再把“标签被占用”当成“消息属于当前分支”。fork 仍共享父索引，无需复制父线全部历史。

实际定向测试覆盖：父 A/B/C/D → 从 B 分叉 → 子线追加 E/F → 非法 reorder 替换成 C/D；完整命令和 delta 两种入口均拒绝，发布 root、逻辑状态和操作记录不变。重开和完整恢复后继续拒绝；已确认结果仍可幂等取得，父线快照保持。合法 E/F 重排、明确 import 采用父线旧原文仍可成功，重复成员拒绝。并有原 T-02 oracle 的独立成员集合判定对照。

## 4. P3-R2：共享依赖及资源预算——通过

`MemoryWork` 在一次固定操作/查询内复用已完成结果，分别管理 validate、fits、graph、status 和 corrections 的 active/done。缓存键区分相应 snapshot/branch/cutoff；不把结果保留到后续提交。完成节点保留子图高度，命中缓存时仍检查当前路径加子图高度，不能因为根枚举顺序或共享子图绕过深度限制。

实际结果：同样 32 个对象、60 条依赖，执行图的逻辑 memory 读取为 **32 次**。实际 PagedDomain 上，校验及 basis 适用性为 **114 次**，状态查询为 **145 次**，不再按路径指数展开。这些数字不是物理读写次数或手机性能指标。

测试同时通过：工作状态/边预算超限、缓存深度、不同 basis、后续 view/正文变化、截止点、父子隔离、真实依赖环、纠错环、合法 checkpoint/advance，以及失败选择/纠错后的状态保持与完整恢复。资源错误不会被状态查询吞成 valid。

接受本轮上限作为可明确拒绝的工程保护：累计状态8192、工作单位131072、深度128。**状态数不是“可存8192条记忆”的产品容量承诺，也不是完整内存字节预算。** 原始对象/活动递归仍受单对象和深度约束；P4继续测真实资源。恢复包若超过新预算或含旧非法重排，应明确失败并保留材料，不能裁剪历史、删纠错或提高阈值伪造通过。

## 5. P4/P5 执行顺序与完成线

沿用 `tasks/T-03-storage-foundation.md`，不另起范围：

1. **P4 索引与候选正确性。** 使用小型中文 marker/人工数值向量，验证索引失败/丢失可重建，正文及确认结果不受损；错误 Story/Branch/版本及过期候选不得进入结果。不建设完整混合召回或调用付费模型。
2. **P4 资源冒烟。** 先固定设备、样本、迭代数、资源上限和停止条件；小集 → 约百万字符1x，覆盖逐轮增长、深层编辑/分叉、冷重开、范围读取和完整恢复。区分业务页、目录/中间页、保留历史和不可达材料的占用，区分逻辑访问、provider读写与原生耗时。上一轮增长样本的高读写/空间放大仍须解释，不能因R2通过而略过。
3. **P5 最小诊断与说明。** 汇总版本、库、Head/view、提交/恢复/索引状态及明确错误；完善安装/启停、备份、故障复现和只读回退。不得扩展完整工作台、品牌装修或正式聊天同步。

1x 完整恢复及规定资源记录仍是原卡的收尾要求；5x/10x只作容量探索，超限安全停止并记录，不声称未测规模可用。延迟预算未确认，因此报告探索性测量，不自定阈值宣称手机达标。已有增长包的正确性结果不能替代本轮指定provider/设备的资源测量。

手机必须另有安全独立环境和许可，否则保留 mobile pending；不测试用户唯一长期档案。自动维护共存仍不在本次放行范围，不能为完成S08而删门禁或把未测写通过。

可以在同一已授权任务内分批测试/提交，无新阻塞不必每批等批准。若发现新的正确性、资源算法或宿主能力缺口，停下受影响路径带证据回审，而不是直接放宽安全规则。资源测量发现需要实质重构时，先说明范围及代码量，不以P4名义静默重写地基。保留现有319项回归，新增测试及结果写回原task；P5收尾后由Chat决定整个T-03的最终状态。
