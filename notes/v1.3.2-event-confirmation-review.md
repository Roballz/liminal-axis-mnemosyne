# 1.3.2 事件概要确认修复

2026-09-25；基线 `work/daily-memory-mvp-baibai` / `b68aebd`，独立工作树 `work/event-overview-review-fix`。状态：implemented_unverified，等待线上 review 和真实宿主/手机验证。

## 原因与改动

- 摘要正文润色产生新的 memory revision。事件成员按稳定 owner 跟到当前摘要，而确认概要仅更新 refs/summarized，保留的 latestProgress.memory 仍指旧 revision，eventView 因而再次标记 needsReview。
- 保留当前与手动确认时，将保留进展的来源限定在本次已确认的有效成员内，按同一 owner 更新引用。进展文字和原剧情时间保持不变，不新增追加史。仅保存编辑、稍后确认仍保留待更新状态。
- AI 更新未返回 latestProgress 时，保留进展也走同一确认逻辑。使用合成模型响应验证，没有调用真实或付费模型。
- 原来源已解除关联且没有同一 owner 的有效成员时拒绝确认，并提示手动选择来源或清空最新进展，防止错误引用别的成员。正文变动导致的来源待审核仍先阻断。
- 按钮改为“保留当前 / 手动更新 / AI更新”，用三列等宽网格固定同排，缩小字号与内边距。版本、manifest 缓存参数及 dist 更新为 1.3.2。

业务及 UI 实现 +30/-7 行，测试 +77/-1 行；另有版本、构建产物和本记录。没有新增持久字段或迁移。

## 有限验证

- 在未修复的源码上运行新增复现：保留、手动、模拟 AI 三条路径均在重新读取后仍待更新，3 个反例按预期失败；原有 5 项通过。
- 修复后受影响的 4 文件 64 项通过（`/tmp/mn-v132-targeted.log`）。覆盖仅改摘要、非确认保存仍待更新、确认后重新同步/读取、v7 导出恢复、进展文字与时间保留、旧摘要保留、再次编辑重新待更新。
- 一次全量 Vitest：29 文件通过；新增的来源解除关联测试因合成旧摘要默认依赖全部前缀，另一成员也待审核而失败。仅修正该测试，先独立审核另一成员，再验证不能挪用其来源；重跑受影响文件 9 项通过。业务源码未再改变，其他 29 文件 419 项结果沿用，不重复全量（`/tmp/mn-v132-tests.log`、`/tmp/mn-v132-review-final.log`）。合计最终 428 项通过。
- timeRel 810、vector depth 12、memory regression 53 断言通过；vue-tsc --noEmit 通过。
- npm run build 通过；JS 825.40 kB，CSS 107.90 kB。生产构建继续关闭 source map，沿用小包上传方式。现有单包体积提示未扩展处理。
- git diff --check 通过。环境未发现浏览器可执行文件，未安装或重复尝试浏览器下载；同排布局由代码检查和 Vue 构建验证，无真实手机布局证据。

## 数据影响与回退

仅用户确认时追加事件修订，不批量改库、不删除旧摘要或事件历史；重新改摘要仍会重新提示待更新。导出格式仍为 v7。可回退本次代码提交，回退会恢复本轮修复前的确认问题。后续核对 1.3.2 完整刷新后保留/手动确认及手机同排显示即可，不启用新任务卡。
