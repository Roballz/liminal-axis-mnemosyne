# T-04 最终 Chat review（受控只读桥接范围）

日期：2026-09-22。审核提交：`5fe27062760a76c0b5cc17a57aa27317de166cd0`。

## 1. 结论

**T-04 在当前受控范围通过，状态升为 verified；T04-R1／R2／R3 及真实落盘补证缺口关闭。** 本回执覆盖 `notes/t-04-chat-review.md`、`notes/t-04-repair-review.md` 和任务卡历史段落中的待审/未完成临时结论，不删除历史失败证据。

S-B 仍为 in_progress：T-04 完成，下一步由 Chat／用户规划 T-05。此 review 不创建任务卡，不授权 Codex 自动进入 T-05，不重开 T-02/T-03。

## 2. 审核依据与执行边界

本轮核对固定提交、相对 `ef67703` 的改动清单、前轮回执、任务卡第12–13节、`native-20260922inspect.json`、`native-20260922resume.json` 及生成最终回执的 `packages/bridge/native-resume.mjs`。业务 Importer、TTSource、事件控制器及分页存储实现相对前轮未改变；新增部分是获准的原会话查询/续传辅助入口、收集器及证据。

**本轮 Chat 仅做源码与已提交证据审阅，没有运行新代码、Node测试、TT、手机或读取私人原文。** 原生结果属于 Codex／用户现场执行并提交的证据，不改称 Chat 独立复测；本轮没有重新计算部署文件哈希。

既有验证组合保留：首轮339项回归；R1/R3/R2候选一次345/345全量（包含原339项）；之后null候选适配另有1项针对性导入/完整分页恢复测试通过。它们不是最终代码重新执行了346项全量。新辅助入口的语法/JSON检查与本次实际运行见任务卡；不为纯文档收口重跑回归。

## 3. 原会话清点、授权续传和内容核验已闭环

清点回执 `evals/t04/native-20260922inspect.json`：沿原namespace，在确认旧owner关闭后查询；正文25/25、摘要0/2、session/binding为partial、phase=assets、pending为空。查询未调用prepare/execute，checkpoint前后相同，close成功；耗时1.363秒。由此确认原先不是完全没落盘，也不是只差核验，确实剩两份摘要未提交。

任务卡第13节记录用户随后明确授权补完摘要。最终回执 `evals/t04/native-20260922resume.json`：

| 核验项 | 结果 |
| --- | --- |
| 原session/绑定/目标 | 沿用原会话；先核对既有匿名标识、partial/assets进度与固定来源指纹 |
| 正文/摘要 | 正文25/25、摘要2/2，binding/session均complete |
| 本次新增操作 | prepare=1、execute=1、operationDelta=1；只提交剩余摘要批次，不重导正文 |
| 正文内容 | 逐条核对25条已发布正文及role，与固定来源一致 |
| 候选回复 | 逐条核对candidate values（包括null空位）及selected，与来源一致 |
| 旧摘要 | 2份asset.data逐项相等，category及snapshot作用域核对通过 |
| 历史和来源 | 正文Head保持原值；再次读取来源后的指纹不变 |
| 待处理请求 | 续传前后均无pending请求 |
| 结束状态 | close被单独await并确认owner.status=closed |
| 时间 | 43.343秒；不是性能基准 |

上述相等结果有实际脚本断言支撑，不是仅把预览计数写入done。源码使用原Importer.resume和原prepare/execute路径；pending为空后首次创建剩余摘要批次，不是给未知旧操作换ID重试。正文Head不变不等于数据库完全未变：本次确实发布了摘要及会话回执，账本增加一项。

本次没有重跑整个导入、原生导出/空库恢复或强杀矩阵；完整分页恢复能力继续使用已提交的桥接集成/恢复证据，不把本次close称为一次额外冷开恢复验证。源写入、模型请求及注入均为0。

## 4. 前轮修复与范围限制

- R1：新段/full-copy/overlap与parent-cutoff/existing-child明确分开，用户确认继承范围；保留旧前缀与已有子线尾部，父截取不引入未来内容。采用前轮源码审查及真实Importer/FakeIO正反例和恢复证据。
- R2：scope+stable_id与locator分离，改名/位置冲突要求确认，持久identity别名兼容已有绑定。不是承诺所有宿主或群聊组合已验证。
- R3：面板复用BridgeEvents，正常追加与regenerate分流，旧摘要通知读取白名单并比较，无关通知无写入。此项采用事件控制器集成证据；本次原生续传不是新的全事件真机矩阵。
- null候选保留槽位与索引，当前mes仍为原文权威，不补造回复。真实25条样本中的缺摘要报告属于源coverage，不是目标丢失结论。
- 旧摘要仍以`legacy-inputs-unproven`及明确作用域承接，不因此升级成完整生成输入已证明的TurnMemory或自动可注入事实；可选物品/地点/生活资料没有因本次两份摘要核验获得新的原生覆盖声明。
- `HOST_MAINTENANCE_UNSUPPORTED`、合作式维护排空、生产手机pending、长期真实档案迁移权限及旧机规模/性能未验证继续有效。本轮不授予生产准入，不建设自动召回、摘要生成或A服务器。

旧native-live演示只能在整段动作边界检查预算，不具备通用逐批自动停止；该限制已在bridge README更正。本次一次性续传在prepare前检查预算、只允许一个剩余摘要批次，已准备请求等待排空，并保留阶段及close结果。它补齐本次证据，不代表旧harness或通用进度UI已被全面改造。此前暂停前为何延迟仍无完整阶段数据，不由43秒单次结果反推原因；不因此追加性能实验。

## 5. 收尾与下一步

保留原源档、原目标库、所有版本和已提交证据。临时酒馆助手全局诊断/续传脚本可由用户禁用，不清库、不再次点击演示、不为文档调整启动新测试。

当前完成的是安全承接原文和旧摘要、明确绑定/继承、局部对账与暂停、可恢复导入的受控能力。下一步是T-05的手动正文搜索、来源/旧资料查看及最小工作台规划，不自动进入T-06向量/BM25召回，也不自动创建或执行后续任务卡。
