# 1.3.4 事件输出约定与追加进展限长

2026-09-25；基线 `330f5d0`，独立工作树 `work/event-progress-wording`。状态：implemented_unverified，待线上 review。

- 按用户指定，将 JSON 示例下的 latestProgress.text 说明改为“latestProgress.text 上限为100字，无需写剧情时间”。实际发送协议与前端展示共用该常量，二者同步生效。
- 核查 progress：用于保存本次新成员带来的增量进展，只有非空且存在未整理的新成员时追加到 event_progress；单纯重写已有概要不追加。事件页当前显示 overview、latestProgress 与成员摘要，没有单独渲染 progress 文本；AI 返回 JSON 审阅可见。旧事件缺 overview 时保留从历史 progress 拼接概要/召回节选的兼容逻辑。本次仅解释，未改变这些行为。
- 用户后续决定保留 progress：新写入的 progress 从原4000字符上限收紧为100个 Unicode 字符，提示词明确要求不超过80字。默认概要写法、固定输出协议、旧批处理协议及返回审阅说明同步调整；超过100字拒绝保存，不静默截断，允许在审阅窗口精简后确认。无新增进展仍允许空字符串。
- 不对旧 event_progress 记录追溯施加新长度上限；旧长记录继续读取、备用召回和导出恢复。自动更新间隔、成员进度和历史记录机制不变。
- 相对远端基线，业务/界面实现 +16/-12 行，测试 +31/-1 行；另含版本、manifest、dist及本记录。版本统一为1.3.4，包含之前因上传被拒而留在本地的文案修改；用户本轮“继续”已授权恢复上传。
- 受影响集成2文件54项通过（`/tmp/mn-v134-targeted.log`）。新增检查100字（含非BMP字符）可写、101字创建/更新均拒绝且不产生部分提交、空进展仍可确认；旧2400字记录仍可恢复并遵守召回预算。
- 最终一次 npm test：30文件431项通过；timeRel 810、vector depth 12、memory regression 53断言通过（`/tmp/mn-v134-tests.log`）。vue-tsc --noEmit、npm run build、git diff --check 通过（`/tmp/mn-v134-types.log`、`/tmp/mn-v134-build-final.log`）。使用合成数据与模拟响应，没有调用真实模型或进行宿主实测。
- 无数据库或设置迁移，不修改已有记录或用户自定义提示词。可回退本轮代码恢复旧文案与宽松校验，无下游施工依赖。
